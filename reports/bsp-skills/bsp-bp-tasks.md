# bsp-bp-tasks — DoD-отчёт

> Служебный файл. **Не входит в комплект поставки скила**, на `SKILL.md` не ссылается.
> Хранится только в этом репозитории для аудита качества и улучшения скилов.

## Verification log

Сверка Key methods плана §6.5 против исходного кода `src/cf/CommonModules/БизнесПроцессыИЗадачиСервер/Ext/Module.bsl` и `…/БизнесПроцессыИЗадачиКлиент/Ext/Module.bsl`.

| # | Метод | Сигнатура (план §6.5) | Сигнатура (реальная из Module.bsl) | Статус |
|---|---|---|---|---|
| 1 | `БизнесПроцессыИЗадачиСервер.ВыполнитьЗадачу` | `ВыполнитьЗадачу(ЗадачаСсылка, ДействиеПоУмолчанию = Ложь)` | `Процедура ВыполнитьЗадачу(ЗадачаСсылка, ДействиеПоУмолчанию = Ложь) Экспорт` (строка 752) | ✅ verified дословно |
| 2 | `БизнесПроцессыИЗадачиСервер.ПроконтролироватьЗадачи` | `ПроконтролироватьЗадачи()` | `Процедура ПроконтролироватьЗадачи() Экспорт` (строка 1407) | ✅ verified дословно |
| 3 | `БизнесПроцессыИЗадачиСервер.УведомитьИсполнителейОНовыхЗадачах` | `УведомитьИсполнителейОНовыхЗадачах()` | `Процедура УведомитьИсполнителейОНовыхЗадачах() Экспорт` (строка 1356) | ✅ verified дословно |
| 4 | `БизнесПроцессыИЗадачиСервер.ПеренаправитьЗадачи` | `ПеренаправитьЗадачи(Знач ПеренаправляемыеЗадачи, Знач ИнфоОПеренаправлении, Знач ТолькоПроверка = Ложь, ПеренаправленныеЗадачи)` | `Функция ПеренаправитьЗадачи(Знач ПеренаправляемыеЗадачи, Знач ИнфоОПеренаправлении, Знач ТолькоПроверка = Ложь, ПеренаправленныеЗадачи = Неопределено) Экспорт` (строка 797) | ✅ verified дословно (**Функция**!), 4 параметра |
| 5 | `БизнесПроцессыИЗадачиКлиент.ПеренаправитьЗадачи` | `ПеренаправитьЗадачи(ПеренаправляемыеЗадачи, ФормаВладелец)` | `Процедура ПеренаправитьЗадачи(ПеренаправляемыеЗадачи, ФормаВладелец) Экспорт` (строка 469) | ✅ verified дословно |
| 6 | `БизнесПроцессыИЗадачиСервер.ПринятьЗадачиКИсполнению` | `ПринятьЗадачиКИсполнению(Задачи)` | `Процедура ПринятьЗадачиКИсполнению(Задачи) Экспорт` (строка 1102) | ✅ verified дословно |
| 7 | `БизнесПроцессыИЗадачиСервер.ОстановитьБизнесПроцесс` | `ОстановитьБизнесПроцесс(БизнесПроцесс)` | `Процедура ОстановитьБизнесПроцесс(БизнесПроцесс) Экспорт` (строка 1062) | ✅ verified дословно |
| 8 | `БизнесПроцессыИЗадачиСервер.СделатьАктивнымБизнесПроцесс` | `СделатьАктивнымБизнесПроцесс(БизнесПроцесс)` | `Процедура СделатьАктивнымБизнесПроцесс(БизнесПроцесс) Экспорт` (строка 998) | ✅ verified дословно |
| 9 | `БизнесПроцессыИЗадачиСервер.ЭтоВедущаяЗадача` | `ЭтоВедущаяЗадача(ЗадачаСсылка)` | `Функция ЭтоВедущаяЗадача(ЗадачаСсылка) Экспорт` (строка 916) | ✅ verified дословно (**Функция**!) |
| 10 | `БизнесПроцессыИЗадачиСервер.ФормаВыполненияЗадачи` | `ФормаВыполненияЗадачи(Знач ЗадачаСсылка)` | `Функция ФормаВыполненияЗадачи(Знач ЗадачаСсылка) Экспорт` (строка 703) | ✅ verified дословно (**Функция**!) |

**Итого:** 10/10 ✅ verified дословно, все сигнатуры сверены с исходным кодом модуля.

**Дополнительно сверено (проверка фантомов из плана §6.5):**

- ⚠️ **`БизнесПроцессыИЗадачиСервер.ФормированиеЗадач` — НЕ существует.** Поиск: `grep -nE "ФормированиеЗадач" src/cf/CommonModules/БизнесПроцессыИЗадачиСервер/Ext/Module.bsl` — 0 совпадений. **Удалено из Key methods** (план явно требует не включать). Упомянуто в `## Core concepts` → `### ⚠️ Частые «фантомы»` как анти-паттерн с предупреждением «не вызывать».
- ⚠️ **`БизнесПроцессыИЗадачиСервер.СоздатьЗадачу` — НЕ существует.** Поиск: 0 совпадений. **Удалено из Key methods.** Упомянуто в разделе «Фантомы» с пояснением, что создание задачи — дело конкретного бизнес-процесса (`БизнесПроцессы.<Имя>.СоздатьЗадачу(...)`).
- ⚠️ **`БизнесПроцессыИЗадачиКлиент.ОткрытьФормуЗадачи` — НЕ существует.** Поиск: 0 совпадений. **Удалено из Key methods.** Упомянуто в разделе «Фантомы» с указанием, что для открытия задачи используется стандартный `ОткрытьЗначение(...)` либо `ОткрытьФорму(...)` с параметрами от `БизнесПроцессыИЗадачиСервер.ФормаВыполненияЗадачи`.

**Дополнительно подтверждено:**

- ⚠️ **Общий модуль `БизнесПроцессыИЗадачи` (без суффикса) НЕ существует.** `ls src/cf/CommonModules/ | grep -i бизнеспроцессы` возвращает 7 модулей (все с суффиксами: `…Сервер`, `…Клиент`, `…КлиентСервер`, `…ВызовСервера`, `…Переопределяемый`, `…КлиентПереопределяемый`, `…События`). **Упомянуто в `## Core concepts`** в начале и в Anti-patterns.
- ✅ В `БизнесПроцессыИЗадачиКлиент` есть `ОткрытьДопИнформациюОЗадаче` (строка 498), `ОткрытьБизнесПроцесс` (строка 559), `ОткрытьПредметЗадачи` (строка 571) — подтверждает, что «форма задачи» открывается не через отдельный метод `ОткрытьФормуЗадачи`, а стандартным путём (либо через `ОткрытьЗначение`, либо через `ОткрытьФорму` с параметрами от `ФормаВыполненияЗадачи`).

**Точные строки в исходниках:**

- `src/cf/CommonModules/БизнесПроцессыИЗадачиСервер/Ext/Module.bsl:703` — `Функция ФормаВыполненияЗадачи(Знач ЗадачаСсылка) Экспорт`
- `src/cf/CommonModules/БизнесПроцессыИЗадачиСервер/Ext/Module.bsl:752` — `Процедура ВыполнитьЗадачу(ЗадачаСсылка, ДействиеПоУмолчанию = Ложь) Экспорт`
- `src/cf/CommonModules/БизнесПроцессыИЗадачиСервер/Ext/Module.bsl:797` — `Функция ПеренаправитьЗадачи(Знач ПеренаправляемыеЗадачи, Знач ИнфоОПеренаправлении, Знач ТолькоПроверка = Ложь, ПеренаправленныеЗадачи = Неопределено) Экспорт`
- `src/cf/CommonModules/БизнесПроцессыИЗадачиСервер/Ext/Module.bsl:916` — `Функция ЭтоВедущаяЗадача(ЗадачаСсылка) Экспорт`
- `src/cf/CommonModules/БизнесПроцессыИЗадачиСервер/Ext/Module.bsl:998` — `Процедура СделатьАктивнымБизнесПроцесс(БизнесПроцесс) Экспорт`
- `src/cf/CommonModules/БизнесПроцессыИЗадачиСервер/Ext/Module.bsl:1062` — `Процедура ОстановитьБизнесПроцесс(БизнесПроцесс) Экспорт`
- `src/cf/CommonModules/БизнесПроцессыИЗадачиСервер/Ext/Module.bsl:1102` — `Процедура ПринятьЗадачиКИсполнению(Задачи) Экспорт`
- `src/cf/CommonModules/БизнесПроцессыИЗадачиСервер/Ext/Module.bsl:1356` — `Процедура УведомитьИсполнителейОНовыхЗадачах() Экспорт`
- `src/cf/CommonModules/БизнесПроцессыИЗадачиСервер/Ext/Module.bsl:1407` — `Процедура ПроконтролироватьЗадачи() Экспорт`
- `src/cf/CommonModules/БизнесПроцессыИЗадачиКлиент/Ext/Module.bsl:469` — `Процедура ПеренаправитьЗадачи(ПеренаправляемыеЗадачи, ФормаВладелец) Экспорт`

## Self-check §9.1

| # | Пункт | Статус | Пояснение |
|---|---|---|---|
| 1 | Файл `.claude/skills/bsp-bp-tasks/SKILL.md` существует | ✅ | Создан, 383 строки (≤ 500 лимит) |
| 2 | Frontmatter валиден (name, description, metadata) | ✅ | `name: bsp-bp-tasks`, `description` только триггеры, начинается с `Use this skill when…`, 886 символов (≤ 1024), без кириллицы, third person. `metadata` заполнен: `scope/version/layer/related_subsystems/related_skills` |
| 3 | `## When to use` содержит ≥ 3 типичных сценария | ✅ | 8 сценариев (глагольная форма: «программно выполнить задачу», «перенаправить одну или несколько задач», «открыть из списка задач управляемую форму», «принять / отменить принятие», «остановить / возобновить», «узнать параметры формы», «проверить, является ли задача ведущей», «вызвать обработчик регламентного задания») |
| 4 | `## Не использовать, если` содержит ≥ 2 анти-триггера | ✅ | 7 анти-триггеров (общие утилиты, открытие списка задач, проектирование маршрута, переопределение поведения, проверка подсистемы, фоновая обёртка, отправка e-mail) |
| 5 | `## Core concepts` объясняет архитектуру | ✅ | Семейство модулей, контекст вызова, структура `ИнфоОПеренаправлении`, регламентные задания — точки входа, явный раздел «Фантомы» с 3 предупреждениями и раздел про отсутствующий `БизнесПроцессыИЗадачи` (без суффикса) |
| 6 | Таблица `## Key methods` 10 строк (7-15 для L2), все колонки, `(уточнить)` запрещён | ✅ | 10 строк. Колонки: Метод / Сигнатура / Сервер-Клиент / Назначение / Пример вызова. Все сигнатуры дословно сверены с `Module.bsl`, `(уточнить)` отсутствует |
| 7 | Каждая строка Key methods сверена по двум источникам | ✅ | см. Verification log выше — 10/10 сверены с `Module.bsl`, дополнительно проверены doc-файлы `bsp3111_md/706_…`, `bsp3111_md/713_…`, `bsp3111_md/721_…`, `bsp3111_md/723_…`, `bsp3111_md/726_…`, `bsp3111_md/729_…`, `bsp3111_md/730_…`, `bsp3111_md/736_…`, `bsp3111_md/737_…` |
| 8 | `## Patterns` ≥ 3 примеров с BSL-кодом | ✅ | 5 паттернов: выполнение с авто-обработчиком, двухшаговое перенаправление с проверкой, открытие формы перенаправления, остановка/возобновление, получение и открытие формы выполнения |
| 9 | `## Anti-patterns` ≥ 5 пар «❌ / ✅» | ✅ | 6 пар: вызов `БизнесПроцессыИЗадачи` (без суффикса), вызов `ФормированиеЗадач` (фантом), вызов `ОткрытьФормуЗадачи` (фантом), создание задачи без бизнес-процесса, вызов серверного `ПеренаправитьЗадачи` с клиента, отсутствие `ТолькоПроверка = Истина` |
| 10 | `## How to explore deeper` для L2: имена модулей + ≥ 1 grep + ≥ 1 glob | ✅ | 7 модулей семейства (включая `Subsystems/.../БизнесПроцессыИЗадачи.xml`) + 4 grep-шаблона + 3 glob-маски + mermaid-диаграмма |
| 11 | Нет ссылок на bsp3111_md, its.1c.ru, 1c.ru, src/cf, нет бинарных ассетов | ✅ | В SKILL.md — только имена модулей (через `CommonModules/...` / `Subsystems/...` без привязки к `src/cf/`), имена скилов, номера строк **не упоминаются**. Проверено: `grep -nE "bsp3111_md|its\.1c\.ru|1c\.ru|src/cf" SKILL.md` — 0 совпадений |
| 12 | Объём SKILL.md ≤ 500 строк / 5000 токенов | ✅ | 383 строки (целевой предел 500) |
| 13 | Self-review по чек-листу `prompt-crafting-guide` | ✅ | Description оптимизирован под CSO (только триггеры, ≤ 1024 симв., без workflow-summary), no menus, no key-binding lists, only specific BSL code examples, ошибки с явной диагностикой, defaults given (например, `ДействиеПоУмолчанию = Ложь`, `ТолькоПроверка = Ложь`) |

## Baseline log (RED-фаза)

**Не проводился в этой итерации.** RED/GREEN-прогоны через `opencode-runner/test-harness.sh` — задача следующей итерации после развёртывания скила. Текущая итерация — статическая верификация (Verification log) + ручной self-check.

См. инструкции §9.0 (3 задачи из `## When to use` + `--runs 3` majority vote для устойчивых метрик на flaky-моделях, в той же `--dir` для RED и GREEN).

## Smoke-test log (GREEN-фаза)

**Не проводился в этой итерации.** Запуск `test-harness.sh` — задача следующей итерации.

## REFACTOR log

**Anti-patterns, закрытые при написании скила** (на основании сверки с планом §6.5):

1. ✅ Закрыт фантом `ФормированиеЗадач` — не включён в Key methods, явно упомянут в Core concepts и Anti-patterns с пояснением, что за формирование задач отвечает код бизнес-процесса, а не общий модуль.
2. ✅ Закрыт фантом `СоздатьЗадачу` — не включён в Key methods, упомянут в Core concepts и Anti-patterns.
3. ✅ Закрыт фантом `ОткрытьФормуЗадачи` — не включён в Key methods, упомянут в Core concepts и Anti-patterns с указанием существующих реальных методов клиентского модуля (`ОткрытьДопИнформациюОЗадаче`, `ОткрытьБизнесПроцесс`, `ОткрытьПредметЗадачи`) и рекомендуемого пути (`ОткрытьЗначение` или `ОткрытьФорму` с параметрами от `ФормаВыполненияЗадачи`).
4. ✅ Закрыт фантом модуля `БизнесПроцессыИЗадачи` (без суффикса) — упомянут в Core concepts (в начале раздела), в таблице семейства модулей (строка про `БизнесПроцессыИЗадачиСервер` как обязательный суффикс) и в Anti-patterns.
5. ✅ Уточнена сигнатура `ПеренаправитьЗадачи` (сервер) — явно помечена как **Функция** (а не Процедура) с возвращаемым `Булево`, 4 параметра (включая `ТолькоПроверка` и `ПеренаправленныеЗадачи`).
6. ✅ Уточнена природа `ПеренаправитьЗадачи` (клиент) — обёртка, открывающая управляемую форму, **не** дублирующая серверный метод; внутри делает серверный вызов проверки через `БизнесПроцессыИЗадачиВызовСервера.ПеренаправитьЗадачи(..., Истина)`.
7. ✅ Уточнён контекст вызова всех методов `БизнесПроцессыИЗадачиСервер` — недоступны с тонкого клиента (только Сервер / Толстый клиент / Внешнее соединение); для вызова с клиента — `БизнесПроцессыИЗадачиКлиент` или `БизнесПроцессыИЗадачиВызовСервера`.
8. ✅ Уточнено назначение `ПроконтролироватьЗадачи` / `УведомитьИсполнителейОНовыхЗадачах` — обработчики регламентных заданий `МониторингЗадач` / `УведомлениеИсполнителейОНовыхЗадачах` соответственно; запускать вручную обычно не нужно.
9. ✅ Уточнена природа `ЭтоВедущаяЗадача` и `ФормаВыполненияЗадачи` — **Функции** (возвращают `Булево` / `Структура`), а не Процедуры.
10. ✅ Идиоматический приём «сначала `ТолькоПроверка = Истина`, потом `ТолькоПроверка = Ложь`» вынесен в Pattern 2 и подкреплён в Anti-patterns (п.6).
