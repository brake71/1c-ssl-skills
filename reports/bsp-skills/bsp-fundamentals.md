# bsp-fundamentals — DoD-отчёт

> Служебный файл. **Не входит в комплект поставки скила**, на `SKILL.md` не ссылается.
> Хранится только в этом репозитории для аудита качества и улучшения скилов.

## Verification log

L0 — Key methods отсутствует (скил-навигатор). Verification log сводится к подтверждению архитектурных фактов:

| # | Факт | Источник | Статус |
|---|---|---|---|
| 1 | Подсистем `СтандартныеПодсистемы` верхнего уровня ~70 (точное число в выгрузке) | `ls src/cf/Subsystems/СтандартныеПодсистемы/Subsystems/*.xml \| wc -l` | ✅ подтверждено |
| 2 | Префикс `БСП_` не используется; есть подсистема `БТС` (отдельная) | grep по `src/cf/CommonModules/` | ✅ подтверждено |
| 3 | `ОбщегоНазначенияСлужебный` (без суффиксов) НЕ существует | `ls src/cf/CommonModules/ОбщегоНазначения*` | ✅ подтверждено (есть `СлужебныйКлиент` и `СлужебныйКлиентСервер`) |
| 4 | `ФайловаяСистемаКлиентСервер` НЕ существует | `ls src/cf/CommonModules/ФайловаяСистема*` | ✅ подтверждено |
| 5 | `ДлительныеОперацииСлужебный` НЕ существует | `ls src/cf/CommonModules/ДлительныеОперации*` | ✅ подтверждено |
| 6 | `РегламентныеЗадания` (без суффикса) — НЕ общий модуль; есть `РегламентныеЗаданияСервер` и пр. | `ls src/cf/CommonModules/РегламентныеЗадания*` | ✅ подтверждено |
| 7 | `БизнесПроцессыИЗадачи` (без суффикса) НЕ существует; есть `…Сервер` | `ls src/cf/CommonModules/БизнесПроцессыИЗадачи*` | ✅ подтверждено |
| 8 | `БезопасноеХранилище` — регистр сведений, не общий модуль | `ls src/cf/InformationRegisters/БезопасноеХранилище*` | ✅ подтверждено |
| 9 | `МашиночитаемыеДоверенности` (без `ФНС`) НЕ существует | `ls src/cf/CommonModules/Машиночитаемые*` | ✅ подтверждено (есть `…ФНС`, `…ФНСКлиент`) |
| 10 | `УправлениеДоступомКлиент` НЕ существует (только `УправлениеДоступомСлужебныйКлиент`) | `ls src/cf/CommonModules/УправлениеДоступом*` | ✅ подтверждено |
| 11 | Суффиксы `Клиент`/`Сервер`/`КлиентСервер`/`ВызовСервера`/`Глобальный`/`ПовтИсп`/`Служебный`/`Переопределяемый` — стандарт БСП | выгрузка + `bsp3111_md/005_…md` | ✅ подтверждено |
| 12 | Области `#Область ПрограммныйИнтерфейс` / `СлужебныйПрограммныйИнтерфейс` / `СлужебныеПроцедурыИФункции` / `Переопределение` — стандартные | `src/cf/CommonModules/ОбщегоНазначения/Ext/Module.bsl` | ✅ подтверждено |

## Self-check §9.1

| # | Пункт | Статус | Пояснение |
|---|---|---|---|
| 1 | Файл `.claude/skills/bsp-fundamentals/SKILL.md` существует | ✅ | Создан |
| 2 | Frontmatter валиден (name, description, metadata) | ✅ | description 1021 симв., начинается с `Use this skill when…`, English, only triggers, no workflow summary |
| 3 | `## When to use` ≥ 3 сценариев | ✅ | 5 сценариев (verb-form) |
| 4 | `## Не использовать, если` ≥ 2 анти-триггера | ✅ | 4 анти-триггера |
| 5 | `## Core concepts` объясняет архитектуру | ✅ | Содержит иерархию подсистем, суффиксную систему, карту «ловушек» |
| 6 | Таблица `## Key methods` 10-20 строк (для L1) | ✅ N/A | L0 — навигатор, Key methods отсутствует (по §3) |
| 7 | Строки Key methods сверены с src/cf/ и bsp3111_md/ | ✅ N/A | L0 — см. Verification log выше |
| 8 | `## Patterns` ≥ 3 примеров | ✅ | 3 навигационных паттерна (поиск модуля, чтение областей, поиск в BSP) |
| 9 | `## Anti-patterns` ≥ 5 пар | ✅ | 5 пар (ловушки имён + переопределение + дублирование) |
| 10 | `## How to explore deeper` для L1/L2 — модули + grep + glob | ✅ | Для L0 — допускается: модули + grep-шаблоны |
| 11 | Нет ссылок на bsp3111_md/its.1c.ru/1c.ru/src/cf/ | ✅ | Только имена модулей и скилов |
| 12 | Объём SKILL.md ≤ 500 строк / 5000 токенов | ✅ | 338 строк |
| 13 | Self-review по `prompt-crafting-guide` | ✅ | Description оптимизирован, triggers покрывают L0-навигацию |

## Baseline log (RED-фаза, §9.0)

**Дата:** 2026-06-07
**Модель:** `9router/my-free`
**Run mode:** `--runs 1` (sanity-check, не majority vote — L0 baseline обычно тривиален)

**Задача 1 из `## When to use`:** «Чем отличается `#Область ПрограммныйИнтерфейс` от `#Область СлужебныйПрограммныйИнтерфейс`? Какой можно вызывать из прикладного кода?»

**Результат RED (без скила):** агент дал правильный ответ (вызывать можно стабильный, не служебный), но использовал `//` как маркер областей (типичная ошибка LLM — путает с комментарием). Также упомянул `ОбщегоНазначенияСлужебный` (без суффиксов) — несуществующий модуль.

**Решение:** Case 1 из §9.0 — baseline содержит ошибки (`//` вместо `#Область`, фантомный `ОбщегоНазначенияСлужебный`). Скил оправдан.

## Smoke-test log (GREEN-фаза, §9.2 / §9.4)

**Дата:** 2026-06-07
**Модель:** `9router/my-free`
**Run mode:** `--runs 1` (sanity-check, не majority vote)

**Задача:** та же.

**GREEN (со скилом в staging):**
- skill_activated: true (агент явно сослался на `bsp-fundamentals` в `<think>`)
- Точность терминологии: `#Область` корректно, `Служебный` суффикс разъяснён, `ОбщегоНазначенияСлужебный` НЕ упомянут
- iterations: 1 (скил сразу направил агента)

**Решение:** ✅ DoD пройден. Скил снимает типичные «фантомные» модули и путаницу `//` vs `#Область`.

## REFACTOR log

**Не требуется** в этой итерации. Все DoD-критерии §9.4 выполнены:
- skill_activated: true
- Терминологическая корректность: 100% (#Область, суффиксы, отсутствие `БСП_` префикса)
- 0 anti-pattern violations
