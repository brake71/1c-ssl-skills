# bsp-multilang — DoD-отчёт

> Служебный файл. **Не входит в комплект поставки скила**, на `SKILL.md` не ссылается.
> Хранится только в этом репозитории для аудита качества и улучшения скилов.

## Verification log

Сверка Key methods скила `bsp-multilang` против `src/cf/CommonModules/<М>/Ext/Module.bsl` и `bsp3111_md/099_…114_*.md` (все строки `#NNN_имя_метода.md` подтверждают сигнатуру).

| # | Метод | Сигнатура в SKILL.md | Реальная сигнатура | Статус |
|---|---|---|---|---|
| 1 | `МультиязычностьСервер.ПриСозданииНаСервере` | `ПриСозданииНаСервере(Форма, Объект = Неопределено, ИмяОбъекта = Неопределено)` | `Процедура ПриСозданииНаСервере(Форма, Объект = Неопределено, ИмяОбъекта = Неопределено) Экспорт` (line 51, `#Область ПрограммныйИнтерфейс`, ends at 537) | ✅ verified дословно |
| 2 | `МультиязычностьСервер.ПриЧтенииНаСервере` | `ПриЧтенииНаСервере(Форма, ТекущийОбъект, ИмяОбъекта = Неопределено)` | `Процедура ПриЧтенииНаСервере(Форма, ТекущийОбъект, ИмяОбъекта = Неопределено) Экспорт` (line 135, `#Область ПрограммныйИнтерфейс`) | ✅ verified дословно |
| 3 | `МультиязычностьСервер.ПередЗаписьюНаСервере` | `ПередЗаписьюНаСервере(ТекущийОбъект)` | `Процедура ПередЗаписьюНаСервере(ТекущийОбъект) Экспорт` (line 167, `#Область ПрограммныйИнтерфейс`) | ✅ verified дословно |
| 4 | `МультиязычностьСервер.ПриЧтенииПредставленийНаСервере` | `ПриЧтенииПредставленийНаСервере(Объект)` | `Процедура ПриЧтенииПредставленийНаСервере(Объект) Экспорт` (line 285, `#Область ПрограммныйИнтерфейс`) | ✅ verified дословно |
| 5 | `МультиязычностьСервер.ОбработкаПолученияДанныхВыбора` | `ОбработкаПолученияДанныхВыбора(ДанныеВыбора, Знач Параметры, СтандартнаяОбработка, ОбъектМетаданных)` | `Процедура ОбработкаПолученияДанныхВыбора(ДанныеВыбора, Знач Параметры, СтандартнаяОбработка, ОбъектМетаданных) Экспорт` (line 378, `#Область ПрограммныйИнтерфейс`) | ✅ verified дословно |
| 6 | `МультиязычностьСервер.ИзменитьПолеЗапросаПодТекущийЯзык` | `ИзменитьПолеЗапросаПодТекущийЯзык(ТекстЗапроса, ИмяПоля)` | `Процедура ИзменитьПолеЗапросаПодТекущийЯзык(ТекстЗапроса, ИмяПоля) Экспорт` (line 425, `#Область ПрограммныйИнтерфейс`) | ✅ verified дословно |
| 7 | `МультиязычностьКлиентСервер.СуффиксЯзыка` | `СуффиксЯзыка(ПорядковыйНомерЯзыка = Неопределено)` | `Функция СуффиксЯзыка(ПорядковыйНомерЯзыка = Неопределено) Экспорт` (line 19, `#Область ПрограммныйИнтерфейс`) | ✅ verified дословно |
| 8 | `МультиязычностьСервер.СуффиксЯзыка` | `СуффиксЯзыка(КодЯзыка)` | `Функция СуффиксЯзыка(КодЯзыка) Экспорт` (line 461, `#Область ПрограммныйИнтерфейс`) | ✅ verified дословно |
| 9 | `МультиязычностьКлиентСервер.ОбработкаПолученияПредставления` | `ОбработкаПолученияПредставления(Данные, Представление, СтандартнаяОбработка, ИмяРеквизита = "Наименование")` | `Процедура ОбработкаПолученияПредставления(Данные, Представление, СтандартнаяОбработка, ИмяРеквизита = "Наименование") Экспорт` (line 40, `#Область ПрограммныйИнтерфейс`) | ✅ verified дословно |
| 10 | `МультиязычностьСервер.ЗаполнитьМультиязычныйРеквизит` | `ЗаполнитьМультиязычныйРеквизит(Элемент, ИмяРеквизита, ИсходнаяСтрока, КодыЯзыков = Неопределено)` | `Процедура ЗаполнитьМультиязычныйРеквизит(Элемент, ИмяРеквизита, ИсходнаяСтрока, КодыЯзыков = Неопределено) Экспорт` (line 25, `#Область ПрограммныйИнтерфейс`) | ✅ verified дословно |

**Дополнительно сверено:**

- Подтверждено наличие `МультиязычностьКлиент.ПриОткрытии` в `#Область ПрограммныйИнтерфейс` (line 20) — упомянут в Core concepts и How to explore deeper, в Key methods не включён (формат «обработчик события», а не самостоятельный API; в L3-скиле 10 строк Key methods достаточно).
- Подтверждено отсутствие `МультиязычностьКлиентСервер.ОбработкаПолученияПолейПредставления` в Key methods скила (присутствует в Core concepts) — в `МультиязычностьКлиентСервер/Ext/Module.bsl:112`, `#Область ПрограммныйИнтерфейс`. Не включён в таблицу ради лимита L3 (10 строк), но в Pattern 2 скила он показан.
- Подтверждено наличие `МультиязычностьСервер.СведенияОЯзыках`, `МультиязычностьСервер.КоличествоДополнительныхЯзыков`, `МультиязычностьСервер.СуффиксТекущегоЯзыка`, `МультиязычностьСервер.ИспользуетсяДополнительныйЯзык`, `МультиязычностьСервер.ИменаРеквизитовСУчетомКодаЯзыка` — все в `#Область ПрограммныйИнтерфейс` (`МультиязычностьСервер/Ext/Module.bsl` lines 446, 479, 496, 515, 532). В Key methods включён только `СуффиксЯзыка` (по коду языка) — он покрывает большинство сценариев; `СуффиксТекущегоЯзыка` упомянут в Anti-patterns. Остальные описаны в Core concepts как информационные.
- Подтверждено, что `#Область СлужебныйПрограммныйИнтерфейс` `МультиязычностьСервер` (line 538) содержит ~10 функций (`ТребуетсяУстановкаРегиональныхНастроекИнформационнойБазы`, `КодОсновногоЯзыка`, `ПредставлениеЯзыка`, `ОбъектСодержитТЧПредставления`, `ИмяРеквизитаБезСуффиксаЯзык`, `СведенияОРеквизите`, `ПризнакЛокализуемогоРеквизита`, `МультиязычныеСтрокиВРеквизитах`, `МультиязычныеРеквизитыОбъекта`, `ОбновитьМультиязычныеСтрокиПредопределенныхЭлементов`) — в Key methods **не включены**: модуль `МультиязычностьСервер` не имеет суффикса `*Служебный` (поэтому формально стабилен), но его `#Область СлужебныйПрограммныйИнтерфейс` всё же служебный; L3-скил ограничивается стабильным `#Область ПрограммныйИнтерфейс` (как и `bsp-base-common`).
- Подтверждено отсутствие `*Служебный` модулей семейства `Мультиязычность` (нет `МультиязычностьСерверСлужебный`, `МультиязычностьКлиентСерверСлужебный` и т. п.). Служебный слой представлен через `#Область СлужебныйПрограммныйИнтерфейс` основного серверного модуля.

**Итого:** 10 ✅ verified, 0 ❌, 0 `(уточнить)`.

## Self-check §9.1

| # | Пункт | Статус | Пояснение |
|---|---|---|---|
| 1 | Файл `.claude/skills/bsp-multilang/SKILL.md` существует | ✅ | Создан |
| 2 | Frontmatter валиден (name, description, metadata) | ✅ | `name: bsp-multilang`, `description` — только триггеры, начинается с `Use this skill when…`, без кириллицы, ≤ 1024 симв. (1019 симв.); `metadata.scope/version/layer (L3)/related_subsystems/related_skills` заполнены, `related_skills` включает `bsp-fundamentals`, `bsp-base-common` |
| 3 | `## When to use` содержит ≥ 3 типичных сценария | ✅ | 6 сценариев (глагольная форма: встроить, сформировать, реализовать, обработать, заполнить, программно узнать) |
| 4 | `## Не использовать, если` содержит ≥ 2 анти-триггера | ✅ | 4 анти-триггера (`НСтр`, печатные формы, машинный перевод, региональные настройки) |
| 5 | `## Core concepts` объясняет архитектуру | ✅ | Способы хранения (шапка vs ТЧ), суффиксная система, таблица 3 ключевых модулей, `НСтр` vs `ЗаполнитьМультиязычныйРеквизит`, fallback на основной язык, BCP-47 |
| 6 | Таблица `## Key methods` 5-10 строк (L3), все колонки, `(уточнить)` запрещён | ✅ | 10 строк, все колонки (`Метод`, `Сигнатура`, `Сервер/Клиент`, `Назначение`, `Пример вызова`), 0 `(уточнить)`. Строки из `*Служебный` модулей отсутствуют (таких модулей в семействе нет) |
| 7 | Каждая строка Key methods сверена по двум источникам | ✅ | см. Verification log выше: 10/10 ✅ verified дословно по `src/cf/.../Module.bsl` + подтверждено через `bsp3111_md/NNN_*.md` для каждого метода |
| 8 | `## Patterns` ≥ 3 примеров с BSL-кодом | ✅ | 4 паттерна (форма объекта, модуль менеджера, динамический список, начальное заполнение) |
| 9 | `## Anti-patterns` ≥ 5 пар «❌ / ✅» | ✅ | 6 пар (строковый реквизит с разделителем, хардкод суффикса, ручной fallback, `НСтр` в реквизит, форма списка без вызова БСП, `ПовтИсп` напрямую) |
| 10 | `## How to explore deeper` для L3: имена модулей + ≥ 1 grep-pattern | ✅ | 5 модулей + 3 grep-шаблона (без glob/mermaid — L3 минимум) |
| 11 | Нет ссылок на `bsp3111_md/`, `its.1c.ru`, `1c.ru`, `src/cf/`, нет бинарных ассетов | ✅ | Все ссылки только на имена общих модулей и скилов; нет ни одного URL, пути или бинарного ассета |
| 12 | Объём `SKILL.md` ≤ 500 строк / 5000 токенов | ✅ | 213 строк (целевой L3 диапазон 200-300) |
| 13 | Self-review по чек-листу `prompt-crafting-guide` | ✅ | Description — только триггеры, no workflow summary; keywords (BCP-47, fallback, ОбработкаПолученияПредставления, ПередЗаписьюНаСервере) в описании и When to use; нет menus; defaults даны в Anti-patterns |

## Baseline log (RED-фаза, §9.0)

**Дата:** _pending — RED-фаза не выполняется в этой итерации (статическая верификация по запросу пользователя)._
**Модель:** _pending_
**Версия opencode:** _pending_
**Run mode:** _pending_

_3 задачи из `## When to use` (первые 3) для прогона на следующей итерации:_

1. «Добавь в форму элемента справочника `Номенклатура` возможность ввода наименования на двух языках, с автоподбором в списке на любом из введённых языков.» — ожидаемая ловушка: baseline может предложить ТЧ `Переводы` без `КодЯзыка` или конкатенацию строк.
2. «Сформируй динамический список формы списка, чтобы колонка `Наименование` отображалась на текущем языке пользователя, не дублируя текст запроса в коде формы.» — ожидаемая ловушка: baseline может переопределить `ТекстЗапроса` руками через `СтрЗаменить`/`НРег`.
3. «В модуле менеджера справочника реализуй представление ссылки на текущем языке интерфейса пользователя.» — ожидаемая ловушка: baseline напишет свой `ОбработкаПолученияПредставления` без `МультиязычностьКлиентСервер.*`.

_Решение (case 1 / case 2 / case 3 из §9.0) — будет вынесено в следующую итерацию._

## Smoke-test log (GREEN-фаза, §9.2 / §9.4)

**Дата:** _pending — статическая верификация по запросу пользователя, без прогона._
**Модель:** _pending_
**Версия opencode:** _pending_
**Run mode:** _pending_

_Метрики из §9.4 (method_accuracy, skill_grounding, anti_pattern_violations, total_tokens_delta, skill_activated) — будут собраны на следующей итерации через `references/test-harness.sh` (см. §9.4)._

_Решение: ⏸ отложено до следующей итерации (статический аудит по плану §9.1)._

## REFACTOR log

_Будет заполнен по итогам GREEN-фазы — anti-patterns, требующие усиления (по результатам baseline-разбора)._

_Предварительно закрытые риски на этапе статической верификации:_

- ✅ Анти-паттерн «хардкод `Язык1`» — явно вынесен в Anti-patterns #2 с правильным `МультиязычностьСервер.СуффиксЯзыка(КодЯзыка)`.
- ✅ Анти-паттерн «своё `ОбработкаПолученияПредставления`» — закрыт через Pattern 2, где показано делегирование в `МультиязычностьКлиентСервер.*`.
- ✅ Анти-паттерн «вызов `ПовтИсп` напрямую» — закрыт через Anti-patterns #6.
- ✅ Не путать `#Область СлужебныйПрограммныйИнтерфейс` (служебный API внутри основного модуля) и `*Служебный`-модули (их в семействе нет) — явно отражено в Verification log, ни один метод служебной области не попал в Key methods.
