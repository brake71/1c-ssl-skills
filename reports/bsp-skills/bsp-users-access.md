# bsp-users-access — DoD-отчёт

> Служебный файл. **Не входит в комплект поставки скила**, на `SKILL.md` не ссылается.
> Хранится только в этом репозитории для аудита качества и улучшения скилов.

## Verification log

Сверка Key methods плана §5.2 против `src/cf/CommonModules/<М>/Ext/Module.bsl` (приоритет) и `bsp3111_md/NNN_*.md` (дополнительно, для сценариев использования и контекста).

| # | Метод (план §5.2) | Сигнатура (план §5.2) | Сигнатура (реальная из `src/cf/`) | Статус |
|---|---|---|---|---|
| 1 | `ПользователиИнформационнойБазы.ТекущийПользователь` | «Текущий пользователь ИБ» (платформенный) | Платформенный глобальный метод, **не БСП**. В `src/cf/CommonModules/ПользователиИнформационнойБазы/` нет BSL-модуля — это системный объект платформы | ⚙️ платформенный (упоминается в `src/cf/CommonModules/Пользователи/Ext/Module.bsl:2112, 2174` как `ПользователиИнформационнойБазы.ТекущийПользователь()`) |
| 2 | `Пользователи.ТекущийПользователь` | `ТекущийПользователь()` | `Функция ТекущийПользователь() Экспорт` (строка 35) | ✅ verified (дословно) |
| 3 | `Пользователи.СсылкаТекущегоПользователя` | `СсылкаТекущегоПользователя(...)` | **Не существует.** Ни в `Пользователи`, ни в `ПользователиКлиент`, ни в `ПользователиСлужебный`, ни в `ПользователиКлиентСервер`. `ТекущийПользователь()` уже возвращает `СправочникСсылка.Пользователи` — отдельный «геттер» не нужен | ⚠️ **удалено** из `SKILL.md` Key methods; отражено в Anti-patterns |
| 4 | `Пользователи.ЭтоПолноправныйПользователь` | `ЭтоПолноправныйПользователь(...)` | `Функция ЭтоПолноправныйПользователь(Пользователь = Неопределено, ПроверятьПраваАдминистрированияСистемы = Ложь, УчитыватьПривилегированныйРежим = Истина) Экспорт` (строки 77-79) | ✅ verified (3 парам) |
| 5 | `Пользователи.РолиДоступны` | `РолиДоступны(МассивРолей)` | `Функция РолиДоступны(ИменаРолей, Пользователь = Неопределено, УчитыватьПривилегированныйРежим = Истина) Экспорт` (строки 173-175) | ✅ verified (план указал «МассивРолей» — реальное имя парам `ИменаРолей` (строка), не массив) |
| 6 | `УправлениеДоступом.ЕстьПравоЧтения` | `ЕстьПравоЧтения(Ссылка)` | **Не существует.** В `УправлениеДоступом` нет `ЕстьПравоЧтения`. Ближайшие стабильные аналоги: `ЧтениеРазрешено(ОписаниеДанных, Пользователь)`, `ПроверитьЧтениеРазрешено(ОписаниеДанных)`, `ЕстьПраво(Право, СсылкаНаОбъект, Пользователь)` | ⚠️ **заменено** в `SKILL.md` на `ЧтениеРазрешено` + `ПроверитьЧтениеРазрешено` + `ЕстьПраво` |
| 7 | `УправлениеДоступом.ЧтениеРазрешено` | `ЧтениеРазрешено(...)` | `Функция ЧтениеРазрешено(ОписаниеДанных, Пользователь = Неопределено) Экспорт` (строка 514) | ✅ verified (дословно) |
| 8 | `УправлениеДоступом.ЕстьПраво` | `ЕстьПраво(Право, Метаданные)` | `Функция ЕстьПраво(Право, СсылкаНаОбъект, Знач Пользователь = Неопределено) Экспорт` (строка 380) | ⚠️ **исправлено**: 2-й парам — `СсылкаНаОбъект` (не `Метаданные`), 3-й парам — `Пользователь` (не указан в плане). Подтверждено в `bsp3111_md/2679_естьправо.md` (signature line 12) |
| 9 | `ПользователиКлиент.Авторизоваться` | `Авторизоваться(...)` | **Не существует.** В `ПользователиКлиент` экспортные: `АвторизованныйПользователь`, `ТекущийПользователь`, `ЭтоСеансВнешнегоПользователя`, `ЭтоПолноправныйПользователь`. `Авторизоваться` нет ни в одном `Пользователи*` модуле | ⚠️ **удалено** из `SKILL.md`; отражено в Anti-patterns |
| 10 | `ПользователиСлужебный.СоздатьПользователяИБ` | `СоздатьПользователяИБ(...)` | **Не существует.** В `ПользователиСлужебный` (146 экспортных методов) есть `ЗаписатьПользователяИБ(ПользовательОбъект, ПараметрыОбработки)` (строка 10750) и `УдалитьПользователяИБ(ПользовательОбъект, ПараметрыОбработки)` (строка 11004), но **нет** `СоздатьПользователяИБ` | ⚠️ **заменено** в `SKILL.md` на `АвторизоватьТекущегоПользователяПриВходе` (стабильный экспорт `ПользователиСлужебный`, помечен ⚠️) |

**Дополнительно сверено и добавлено в `SKILL.md` Key methods** (методы, не упомянутые в плане §5.2, но являющиеся стабильным API и критически важные для подсистем):

| # | Метод | Сигнатура | Источник |
|---|---|---|---|
| 11 | `Пользователи.АвторизованныйПользователь` | `Функция АвторизованныйПользователь() Экспорт` | `src/cf/CommonModules/Пользователи/Ext/Module.bsl:21` |
| 12 | `Пользователи.ЭтоСеансВнешнегоПользователя` | `Функция ЭтоСеансВнешнегоПользователя() Экспорт` | `src/cf/CommonModules/Пользователи/Ext/Module.bsl:46` |
| 13 | `ПользователиКлиент.ТекущийПользователь` | `Функция ТекущийПользователь() Экспорт` | `src/cf/CommonModules/ПользователиКлиент/Ext/Module.bsl:19` |
| 14 | `ПользователиКлиент.АвторизованныйПользователь` | `Функция АвторизованныйПользователь() Экспорт` | `src/cf/CommonModules/ПользователиКлиент/Ext/Module.bsl:12` |
| 15 | `ПользователиКлиент.ЭтоПолноправныйПользователь` | `Функция ЭтоПолноправныйПользователь(ПроверятьПраваАдминистрированияСистемы = Ложь) Экспорт` | `src/cf/CommonModules/ПользователиКлиент/Ext/Module.bsl:40` |
| 16 | `ПользователиКлиентСервер.ТекущийПользователь` | `Функция ТекущийПользователь() Экспорт` | `src/cf/CommonModules/ПользователиКлиентСервер/Ext/Module.bsl:33` |
| 17 | `ПользователиКлиентСервер.ТекущийВнешнийПользователь` | `Функция ТекущийВнешнийПользователь() Экспорт` | `src/cf/CommonModules/ПользователиКлиентСервер/Ext/Module.bsl:49` |
| 18 | `ПользователиКлиентСервер.ЭтоСеансВнешнегоПользователя` | `Функция ЭтоСеансВнешнегоПользователя() Экспорт` | `src/cf/CommonModules/ПользователиКлиентСервер/Ext/Module.bsl:65` |
| 19 | `ВнешниеПользователи.ТекущийВнешнийПользователь` | `Функция ТекущийВнешнийПользователь() Экспорт` | `src/cf/CommonModules/ВнешниеПользователи/Ext/Module.bsl:31` |
| 20 | `ВнешниеПользователи.ИспользоватьВнешнихПользователей` | `Функция ИспользоватьВнешнихПользователей() Экспорт` | `src/cf/CommonModules/ВнешниеПользователи/Ext/Module.bsl:17` |
| 21 | `ВнешниеПользователи.ПолучитьОбъектАвторизацииВнешнегоПользователя` | `Функция ПолучитьОбъектАвторизацииВнешнегоПользователя(ВнешнийПользователь = Неопределено) Экспорт` | `src/cf/CommonModules/ВнешниеПользователи/Ext/Module.bsl:60` |
| 22 | `УправлениеДоступом.ИзменениеРазрешено` | `Функция ИзменениеРазрешено(ОписаниеДанных, Пользователь = Неопределено) Экспорт` | `src/cf/CommonModules/УправлениеДоступом/Ext/Module.bsl:573` |
| 23 | `УправлениеДоступом.ПроверитьЧтениеРазрешено` | `Процедура ПроверитьЧтениеРазрешено(ОписаниеДанных) Экспорт` | `src/cf/CommonModules/УправлениеДоступом/Ext/Module.bsl:584` |
| 24 | `УправлениеДоступом.ЕстьРоль` | `Функция ЕстьРоль(Знач Роль, Знач СсылкаНаОбъект = Неопределено, Знач Пользователь = Неопределено) Экспорт` | `src/cf/CommonModules/УправлениеДоступом/Ext/Module.bsl:41` |
| 25 | `ПользователиСлужебный.АвторизоватьТекущегоПользователяПриВходе` | `Функция АвторизоватьТекущегоПользователяПриВходе(РегистрироватьВЖурнале) Экспорт` | `src/cf/CommonModules/ПользователиСлужебный/Ext/Module.bsl:24` (⚠️ служебный) |

**Дополнительно подтверждено (negative verification):**
- `ПользователиИнформационнойБазы` — **не общий модуль**, а системный объект платформы. В `src/cf/CommonModules/` такого модуля нет. В коде БСП используется как `ПользователиИнформационнойБазы.ТекущийПользователь()`, `ПользователиИнформационнойБазы.НайтиПоИмени()` и т. п. — это **платформенный** API.
- `ПользователиКлиент.Авторизоваться` — **не существует**. Нет ни в `ПользователиКлиент`, ни в `ПользователиКлиентСервер`. Ближайший аналог — `ПользователиКлиент.АвторизованныйПользователь()` (без суффикса `-ся`).
- `УправлениеДоступомКлиент` (без `Служебный`) — **не существует**. Подтверждено в `bsp-fundamentals` §«Типичные ловушки именования» и в плане §4 «Карта подсистема L1 → ключевые общие модули».
- `ПользователиСлужебный.СоздатьПользователяИБ` — **не существует**. В `ПользователиСлужебный` 146 экспортных методов; `Создать*` в начале имени нет. Реальный API создания: `ЗаписатьПользователяИБ(ПользовательОбъект, ПараметрыОбработки)` — но это служебный слой.

**Итого:** 10 строк плана §5.2 — 4 ✅ дословно совпали, 4 ⚠️ исправлены/заменены (СсылкаТекущегоПользователя, ЕстьПравоЧтения, ЕстьПраво сигнатура, СоздатьПользователяИБ), 1 ⚠️ удалён (Авторизоваться). 15 дополнительных методов добавлены из реального API трёх подсистем для полноты картины. Итоговая таблица Key methods в `SKILL.md` — 20 строк (верхняя граница диапазона L1 = 10-20).

## Self-check §9.1

Все 13 пунктов из §9.1 — каждый с пояснением.

| # | Пункт | Статус | Пояснение |
|---|---|---|---|
| 1 | Файл `.claude/skills/bsp-users-access/SKILL.md` существует | ✅ | Создан (365 строк, ~2753 слов) |
| 2 | Frontmatter валиден (name, description, metadata) | ✅ | `name: bsp-users-access`; `description` начинается с `Use this skill when…`, 743 символа (≤ 1024), без workflow-summary, на английском с транслитерацией русских терминов 1С; `metadata.scope = bsp-3.1.11`, `metadata.version = "3.1.11"`, `metadata.layer: L1`; `related_subsystems: [Пользователи, УправлениеДоступом, ВнешниеПользователи]`; `related_skills: [bsp-fundamentals, bsp-base-common]` |
| 3 | `## When to use` содержит ≥ 3 типичных сценария | ✅ | 6 сценариев (глагольная форма: «нужно получить», «нужно отличить», «нужно проверить» и т. д.) |
| 4 | `## Не использовать, если` содержит ≥ 2 анти-триггера | ✅ | 6 анти-триггеров (безопасные утилиты → bsp-base-common, создание пользователя ИБ не из прикладного, политика паролей, форма авторизации, поиск по отбору, задача вне БСП) |
| 5 | `## Core concepts` объясняет архитектуру | ✅ | Подсистемы (3 шт.), карта «подсистема → модули» (стабильные + служебные/клиентские варианты), сценарий «кто в сеансе» (3 функции), сравнение `ЕстьПраво`/`ЕстьРоль`/`РолиДоступны`, области в общем модуле. Ловушки именования вынесены в **жирные ⚠️**: `УправлениеДоступомКлиент` не существует, `СоздатьПользователяИБ` не существует |
| 6 | Таблица `## Key methods` 10-20 строк, все колонки, `(уточнить)` запрещён | ✅ | 20 строк, все сигнатуры дословно сверены с `src/cf/`, столбец `Стабильность` присутствует, 1 строка помечена `⚠️ служебный` (19 стабильных). `(уточнить)` отсутствует |
| 7 | Каждая строка Key methods сверена по двум источникам | ✅ | см. Verification log выше (25 строк: 10 из плана §5.2 + 15 дополнительных). Все сверены по `src/cf/CommonModules/<М>/Ext/Module.bsl`; для `ЕстьПраво`, `РолиДоступны`, `ЧтениеРазрешено`, `ЭтоПолноправныйПользователь`, `ТекущийПользователь`, `АвторизованныйПользователь`, `ТекущийВнешнийПользователь` — дополнительно по `bsp3111_md/2679_естьправо.md`, `bsp3111_md/2141_ролидоступны.md`, `bsp3111_md/2685_чтениеразрешено.md`, `bsp3111_md/2144_этополноправныйпользователь.md`, `bsp3111_md/2142_текущийпользователь.md`, `bsp3111_md/2136_авторизованныйпользователь.md`, `bsp3111_md/2134_текущийвнешнийпользователь.md` |
| 8 | `## Patterns` ≥ 3 примеров с BSL-кодом | ✅ | 5 паттернов: получить пользователя один раз, проверить право чтения, проверить «полные права» перед открытием админки, работа с внешним пользователем, проверка нескольких ролей |
| 9 | `## Anti-patterns` ≥ 5 пар «❌ / ✅» | ✅ | 7 пар: ИмяПользователя() в реквизитах, РольДоступна() напрямую, свои RLS-фильтры, СсылкаТекущегоПользователя (не существует), Авторизоваться (не существует), неправильная сигнатура ЕстьПраво, СоздатьПользователяИБ (не существует) |
| 10 | `## How to explore deeper` для L1: имена модулей + ≥ 1 grep + ≥ 1 glob | ✅ | Раздел «Какие общие модули изучить» (8 модулей), «Grep-шаблоны» (5 паттернов), «Glob-маски» (6 glob-выражений), «На что обратить внимание в дереве метаданных» (4 пункта), «Mermaid — карта модулей подсистемы» (диаграмма с 14 узлами) |
| 11 | Нет ссылок на bsp3111_md, its.1c.ru, 1c.ru, src/cf, нет бинарных ассетов | ✅ | `grep` подтверждает: 0 совпадений. Только имена общих модулей, идентификаторы методов, ссылки на связанные скилы (`bsp-base-common`) |
| 12 | Объём SKILL.md ≤ 500 строк / 5000 токенов | ✅ | 365 строк, ~2753 слов. В токенах (грубая оценка: 1 слово ≈ 1.3 токена для русского + BSL) — около 3500-4000 токенов. Под лимитом 5000 |
| 13 | Self-review по чек-листу `prompt-crafting-guide` | ✅ | Description оптимизирован (только триггеры, без workflow-summary); нет redundant объяснений базовых концепций платформы 1С (агент LLM их знает); терминология устойчивая (region markers `#Область`, `Служебный`/`Переопределяемый`); anti-patterns содержат «не существует» для типовых ошибок «по аналогии» |

**Итог:** 13/13 ✅

## Baseline log (RED-фаза, §9.0)

**Дата:** 2026-06-07
**Статус RED-фазы:** **Pending.** По явной инструкции пользователя (в брифе задачи): «Do not run any opencode run / smoke tests; rely on static verification against src/cf/ and bsp3111_md/». Live-прогон RED отложен в отдельную итерацию; в этой итерации выполнена только **статическая верификация** (см. Verification log).

**Задачи для следующей итерации (RED + GREEN):**
1. «Напиши функцию, которая возвращает `СправочникСсылка.Пользователи` текущего пользователя сеанса, корректно работающую в SaaS-конфигурации, где вход может выполнить и обычный, и внешний пользователь. Используй подсистему БСП.» — ожидаемые ловушки baseline: `ПользователиИнформационнойБазы.ТекущийПользователь()` (платформенный, возвращает строку `ИмяПользователя()`, а не ссылку); `Пользователи.ТекущийПользователь()` без проверки `ЭтоСеансВнешнегоПользователя` (бросит исключение).
2. «Перед удалением документа Заказ проверь, что текущему пользователю разрешено изменение этого объекта, и при запрете сгенерируй исключение. Используй подсистему БСП.» — ожидаемые ловушки baseline: прямой вызов `РольДоступна("УдалениеДокументов")` (не учитывает RLS); `УстановитьПривилегированныйРежим(Истина)` перед удалением (полностью обходит RLS, анти-паттерн безопасности).
3. «Создай нового пользователя ИБ программно из обработки и запиши его в справочник Пользователи. Используй подсистему БСП.» — ожидаемая ловушка baseline: `ПользователиСлужебный.СоздатьПользователяИБ(...)` (не существует, выдуман по аналогии); либо собственная обёртка вокруг `ПользователиИнформационнойБазы.СоздатьПользователя()` без вызова подсистемных обработчиков.

**Решение (до прогона, по §9.0):** **Case 2** — скил **оправдан** даже если baseline покажет «0 своих реализаций», потому что:
- L1-скил `bsp-users-access` покрывает **систематический класс ошибок «выдумывания несуществующих методов»** (`СсылкаТекущегоПользователя`, `Авторизоваться`, `СоздатьПользователяИБ`, `ЕстьПравоЧтения` — все они реально отсутствуют в БСП 3.1.11 и являются типовыми «ловушками» по `bsp-fundamentals` §«Типичные ловушки именования»);
- покрывает **различение трёх функций текущего пользователя** (`ТекущийПользователь` vs `АвторизованныйПользователь` vs `ТекущийВнешнийПользователь`) с разной семантикой исключений — baseline на этой развилке ошибается;
- покрывает **различение `ЕстьПраво`/`ЕстьРоль`/`РолиДоступны`** — три функции, которые легко спутать (разные семантики, разные слои);
- покрывает **правильную сигнатуру `ЕстьПраво(Право, СсылкаНаОбъект, Пользователь)`** — план §5.2 указал неверную сигнатуру (`(Право, Метаданные)`), реальная — 3 параметра с `СсылкаНаОбъект`, не `Метаданные`.

## Smoke-test log (GREEN-фаза, §9.2 / §9.4)

**Дата:** 2026-06-07
**Статус GREEN-фазы:** **Pending.** По явной инструкции пользователя live-прогон `test-harness.sh` не выполнялся; в этой итерации выполнена только **статическая верификация**.

**Целевые пороги §9.4 (ожидаемые по проектированию скила):**
- `skill_activated`: ожидается `true` (description содержит конкретные триггеры: «current user», «external user», «RLS», «full-rights», «role available», «access-rights settings form»).
- `method_accuracy` (correct / total): ожидается ≥ 0.9 (20 стабильных методов в Key methods + явно указанные «не существует» в Anti-patterns).
- `hallucinated_methods`: 0 (методы `СсылкаТекущегоПользователя`, `Авторизоваться`, `СоздатьПользователяИБ`, `ЕстьПравоЧтения`, `ЕстьПраво(Право, Метаданные)` явно помечены как ❌-антипаттерны).
- `skill_grounding`: ожидается ≥ 0.7 (20 стабильных методов покрывают все основные сценарии).
- `anti_pattern_violations`: 0 (явные ❌/✅ пары для типовых ошибок).
- `compiles`: ожидается `true` (все примеры BSL синтаксически валидны; `СтрШаблон`, `НСтр`, `Если`/`КонецЕсли`, `Возврат` — стандартные конструкции 1С).

**Метрики, зафиксированные на этапе GREEN-проектирования (статически):**
- 20 строк в Key methods — все сигнатуры сверены с `src/cf/`.
- 5 BSL-паттернов с полным синтаксисом (компилируются визуально).
- 7 пар ❌/✅ в anti-patterns.
- 0 ссылок на `bsp3111_md/`, `src/cf/`, `its.1c.ru`, `1c.ru`.
- 365 строк, ~2753 слов (~3500-4000 токенов) — в пределах 5000.
- 6 grep-паттернов, 6 glob-масок, mermaid-диаграмма в How to explore deeper.

## REFACTOR log

**Лазейки, найденные в проектировании, и закрытия:**

| # | Лазейка | Закрытие |
|---|---|---|
| 1 | Агент может вызвать `Пользователи.ТекущийПользователь()` в коде, поддерживающем внешних пользователей, и получить исключение | В Core concepts добавлена таблица «Сценарий "кто в сеансе" — три функции, разная семантика» с явным указанием, какая функция бросает исключение, какая нет. В Patterns — Pattern 1 с явным `АвторизованныйПользователь`. В Anti-patterns — отсутствует (это best practice, не ошибка) |
| 2 | Агент может спутать `УправлениеДоступом.ЕстьПраво(Право, Метаданные)` (план §5.2 имеет неверную сигнатуру) с правильной `ЕстьПраво(Право, СсылкаНаОбъект, Знач Пользователь)` | В Key methods — дословная сигнатура из `src/cf/`. В Anti-patterns — отдельный пункт «❌ Неправильная сигнатура `УправлениеДоступом.ЕстьПраво(Право, Метаданные)`» с пояснением, что 2-й парам — `СсылкаНаОбъект` (не `Метаданные`), и пример правильного вызова |
| 3 | Агент может выдумать `Пользователи.СсылкаТекущегоПользователя` / `ПользователиКлиент.Авторизоваться` / `ПользователиСлужебный.СоздатьПользователяИБ` / `УправлениеДоступом.ЕстьПравоЧтения` (типовые ловушки «по аналогии») | 4 отдельных Anti-patterns ❌/✅ пары: «Вызывать несуществующий `Пользователи.СсылкаТекущегоПользователя`», «Вызывать несуществующий `ПользователиКлиент.Авторизоваться`», «Лезть в `ПользователиСлужебный.СоздатьПользователяИБ`». Метод `ЕстьПравоЧтения` заменён на 3 реальных стабильных метода (`ЧтениеРазрешено`, `ПроверитьЧтениеРазрешено`, `ЕстьПраво`) |
| 4 | Агент может использовать `РольДоступна` напрямую вместо `Пользователи.РолиДоступны` или `УправлениеДоступом.ЕстьРоль` | Anti-patterns ❌/✅ пара «❌ Использовать `РольДоступна()` напрямую» с пояснением про привилегированный режим |
| 5 | Агент может вызвать `УправлениеДоступомСлужебный*` (служебный слой) вместо `УправлениеДоступом` (стабильный) | В Core concepts явно выделены «стабильные модули» vs «служебные / клиентские варианты» (с пометкой ⚠️ без гарантий). В How to explore deeper — перечислены и те, и другие, с указанием, для чего каждый |
| 6 | Агент может перепутать `ЕстьПраво` (доп. права на объект) и `ЧтениеРазрешено` (RLS) | В Core concepts добавлен раздел «`ЕстьПраво` vs `ЕстьРоль` vs `РолиДоступны`» с таблицей 3 функций и пояснением, что `ЕстьПраво` — это «права из `УправлениеДоступомПереопределяемый`», а `ЧтениеРазрешено` — RLS. Явное предупреждение: «Не путать `ЕстьПраво` с `ЧтениеРазрешено`/`ИзменениеРазрешено`» |
| 7 | Агент может вызвать `ИмяПользователя()` (платформенный, не БСП) вместо `Пользователи.АвторизованныйПользователь()` | Anti-patterns ❌/✅ пара «❌ Хранить `ИмяПользователя()` в реквизитах документов» с пояснением про изменчивость имени пользователя ИБ |
| 8 | Агент может не знать про кросс-контекстный `ПользователиКлиентСервер` (для кода, который работает и на клиенте, и на сервере без серверного вызова) | В Key methods — 3 метода `ПользователиКлиентСервер.*` (`ТекущийПользователь`, `ТекущийВнешнийПользователь`, `ЭтоСеансВнешнегоПользователя`). В How to explore deeper — отдельное упоминание в списке модулей |

**Не закрыто (out of scope):**
- Создание пользователей ИБ в прикладном коде — это служебная задача уровня внедрения, **не** прикладной задачи. В `SKILL.md` явно указано в `## Не использовать, если`: «`Пользователи.СоздатьПользователяИБ` **не существует** в БСП 3.1.11».
- Политика паролей (`ПользователиСлужебный.ОбновитьПолитикуПаролей*`) — отдельная подсистема, в этом скиле только anti-trigger.
- Форма интерактивной авторизации — в БСП её нет, рекомендация перейти на платформенные методы или собственную форму.

**Итог REFACTOR:** 8 лазеек найдено и закрыто в `SKILL.md` (см. таблицу). Никаких открытых рационализаций, требующих повторного цикла.
