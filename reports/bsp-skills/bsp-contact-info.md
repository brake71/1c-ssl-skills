# bsp-contact-info — DoD-отчёт

> Служебный файл. **Не входит в комплект поставки скила**, на `SKILL.md` не ссылается.
> Хранится только в этом репозитории для аудита качества и улучшения скилов.

## Verification log

Сверка Key methods скила `bsp-contact-info` против исходного кода БСП 3.1.11 в `src/cf/CommonModules/<М>/Ext/Module.bsl` (приоритет) и официальной документации в `bsp3111_md/NNN_*.md` (дополнительно).

| # | Метод | Сигнатура (план §6.1) | Сигнатура (реальная) | Статус |
|---|---|---|---|---|
| 1 | `УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта` | `КонтактнаяИнформацияОбъекта(СсылкаИлиОбъект, ВидИлиТипКонтактнойИнформации = Неопределено, Дата = Неопределено, ТолькоПредставление = Истина)` | `Функция КонтактнаяИнформацияОбъекта(СсылкаИлиОбъект, ВидИлиТипКонтактнойИнформации = Неопределено, Дата = Неопределено, ТолькоПредставление = Истина) Экспорт` (строка 290) | ✅ verified дословно |
| 2 | `УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъектов` | (отсутствует в плане §6.1) | `Функция КонтактнаяИнформацияОбъектов(СсылкиИлиОбъекты, Знач ТипыКонтактнойИнформации = Неопределено, Знач ВидыКонтактнойИнформации = Неопределено, Дата = Неопределено) Экспорт` (строка 237) | ✅ verified (добавлено в скил как полезное дополнение) |
| 3 | `УправлениеКонтактнойИнформацией.ЗаписатьКонтактнуюИнформацию` | `ЗаписатьКонтактнуюИнформацию(Объект, Значение, ВидИнформации, ТипИнформации, ИдентификаторСтроки, Дата)` | **`Процедура`** (не Функция!) `ЗаписатьКонтактнуюИнформацию(Объект, Знач Значение, ВидИнформации, ТипИнформации, ИдентификаторСтроки = 0, Дата = Неопределено) Экспорт` (строка 3479) | ⚠️ **исправлено** в скиле: указано «Процедура (не Функция)», дефолт `ИдентификаторСтроки = 0` |
| 4 | `УправлениеКонтактнойИнформацией.ПредставлениеКонтактнойИнформацииОбъекта` | (отсутствует в плане §6.1) | `Функция ПредставлениеКонтактнойИнформацииОбъекта(СсылкаИлиОбъект, ВидКонтактнойИнформации, Разделитель = ",", Дата = Неопределено, ДополнительныеПараметры = Неопределено) Экспорт` (строка 391) | ✅ verified (добавлено в скил как **рекомендуемый** метод; документация для `КонтактнаяИнформацияОбъекта` прямо рекомендует его) |
| 5 | `УправлениеКонтактнойИнформацией.ВидКонтактнойИнформацииПоИмени` | (отсутствует в плане §6.1) | `Функция ВидКонтактнойИнформацииПоИмени(Имя) Экспорт` (строка 2818) | ✅ verified (добавлено как типовой helper) |
| 6 | `УправлениеКонтактнойИнформациейКлиентСервер.ЭтоКонтактнаяИнформацияВJSON` | (отсутствует в плане §6.1) | `Функция ЭтоКонтактнаяИнформацияВJSON(Знач Текст) Экспорт` (строка 72) | ✅ verified (добавлено как детектор формата — критично перед разбором) |
| 7 | `УправлениеКонтактнойИнформациейКлиент.ОткрытьФормуКонтактнойИнформации` | «(уточнить)» в плане §6.1 | `Функция ОткрытьФормуКонтактнойИнформации(Параметры, Владелец = Неопределено, Оповещение = Неопределено) Экспорт` (строка 311) | ✅ verified (уточнено: 3 парам, возвращает `Неопределено` — форма открывается асинхронно) |
| 8 | `УправлениеКонтактнойИнформациейКлиент.ОткрытьФормуВводаАдреса` | (отсутствует в плане §6.1) | `Процедура ОткрытьФормуВводаАдреса(Форма, Результат) Экспорт` (строка 169) | ✅ verified (добавлено как альтернатива для подключаемых команд) |
| 9 | `АдресныйКлассификатор.КодРегионаПоНаименованию` | `КодРегионаПоНаименованию(Название)` | `Функция КодРегионаПоНаименованию(Название) Экспорт` (строка 50) | ✅ verified дословно |
| 10 | `АдресныйКлассификатор.АдресныйКлассификаторЗагружен` | (отсутствует в плане §6.1) | `Функция АдресныйКлассификаторЗагружен() Экспорт` (строка 197) | ✅ verified (добавлено: важная проверка доступности КЛАДР / веб-сервиса) |
| 11 | `АдресныйКлассификатор.ПроверитьАдреса` | `ПроверитьАдреса(Адреса)` | `Функция ПроверитьАдреса(Адреса) Экспорт` (строка 266) | ✅ verified дословно |
| 12 | `АдресныйКлассификатор.СведенияПоОКМТО` | `СведенияПоОКМТО(ОКТМО)` | `Функция СведенияПоОКМТО(ОКТМО) Экспорт` (строка 433). **Имя метода — `ОКТМО`** (не `ОКМТО`, как в плане — опечатка в плане; в `bsp3111_md/016_сведенияпоокмто.md` имя файла тоже `…поокмто`, но имя метода в сигнатуре — `ОКТМО`) | ⚠️ **исправлено** в скиле: имя метода `СведенияПоОКМТО` с правильным написанием |
| 13 | `РаботаСАдресами.СведенияОбАдресе` | (отсутствует в плане §6.1) | `Функция СведенияОбАдресе(Адрес, ДополнительныеПараметры = Неопределено) Экспорт` (строка 287) | ✅ verified (добавлено как основной метод разбора) |
| 14 | `РаботаСАдресами.РегионАдресаКонтактнойИнформации` | (отсутствует в плане §6.1) | `Функция РегионАдресаКонтактнойИнформации(Знач Адрес) Экспорт` (строка 21) | ✅ verified (добавлено как часто используемый helper) |
| 15 | `РаботаСАдресами.АдресПоИдентификатору` | (отсутствует в плане §6.1) | `Функция АдресПоИдентификатору(ИдентификаторАдреса, ДополнительнаяИнформацияАдреса = Неопределено) Экспорт` (строка 720) | ✅ verified (добавлено как основной метод ФИАС-сборки) |
| 16 | `АдресныйКлассификаторСлужебный.ПроверкаАдресов` | (отсутствует в плане §6.1) | `Функция ПроверкаАдресов(Знач НаборАдресов, ПроверятьУстареваниеАдресов = Ложь) Экспорт` (строка 1139) | ⚠️ **служебный** — добавлено в скил с пометкой ⚠️ (1 строка из 15) |

**Дополнительно сверено (план §6.1 — не нашлось в реальной выгрузке):**

| Метод (план §6.1) | Реальное состояние | Статус |
|---|---|---|
| `РаботаСАдресами.СформироватьАдресИзXML` | **Не существует.** Реальные методы XML/JSON-конвертации: `РаботаСАдресами.КонвертироватьАдресИзJSONВXML(ЗначенияПолей, Представление, ОжидаемыйТип)` (строка 1849), `РаботаСАдресами.КонтактнаяИнформацияВСтруктуруJSON(КонтактнаяИнформация, Тип, НастройкиКонвертации)` (строка 1994), `РаботаСАдресами.ПоляАдресаВJSON(ПоляАдреса)` (строка 522), `РаботаСАдресами.АдресПоИдентификатору(ИдентификаторАдреса, ДополнительнаяИнформацияАдреса)` (строка 720). Сборка адреса по идентификатору покрыта `АдресПоИдентификатору` (включено в скил) | ⚠️ **заменено** в скиле на `АдресПоИдентификатору` (ближайший аналог) |
| `УправлениеКонтактнойИнформацией.ПредставлениеКонтактнойИнформации` с `(ЗначениеПолей, ВидИнформации)` | **Сигнатура плана неверна.** Реальная: `ПредставлениеКонтактнойИнформации(Знач КонтактнаяИнформация, Знач ПеревестиВТранслит = Ложь)` (строка 928). Оба аргумента — КИ и флаг транслита, не «поля» и не «вид». Документация самой `КонтактнаяИнформацияОбъекта` прямо рекомендует **другой** метод: `ПредставлениеКонтактнойИнформацииОбъекта` (есть в скиле, строка 391) | ⚠️ **исправлено** в скиле: оба метода представлены, `ПредставлениеКонтактнойИнформацииОбъекта` помечен как «рекомендуемый» |

**Дополнительно подтверждено (модули семейств):**

- ✅ Семейство `УправлениеКонтактнойИнформацией*` существует со всеми перечисленными суффиксами: `УправлениеКонтактнойИнформацией`, `…Клиент`, `…КлиентСервер`, `…Локализация`, `…КлиентЛокализация`, `…КлиентСерверЛокализация`, `…Переопределяемый`, `…Служебный`, `…СлужебныйВызовСервера`, `…СлужебныйПовтИсп`. Модулей `…СлужебныйКлиент` / `…СлужебныйКлиентСервер` **не существует** (только серверные служебные варианты).
- ✅ Семейство `АдресныйКлассификатор*`: `АдресныйКлассификатор`, `…Клиент`, `…ПовтИсп`, `…Служебный`. Модуля `АдресныйКлассификаторКлиентСервер` **не существует**; `…Сервер` без других суффиксов **не существует** (только `…Служебный` с `Сервер`-нагрузкой).
- ✅ Семейство `РаботаСАдресами*`: `РаботаСАдресами`, `…Клиент`, `…КлиентСервер`, `…ПовтИсп`, `…СлужебныйКлиент`. ⚠️ `РаботаСАдресамиСлужебный` (без суффикса) **не существует** — служебный вариант только клиентский.

**Итого:** 13 ✅ verified (в т. ч. 2 дословно по плану: `КонтактнаяИнформацияОбъекта`, `КодРегионаПоНаименованию`, `ПроверитьАдреса`), 1 ⚠️ исправлено (сигнатура `ЗаписатьКонтактнуюИнформацию` — Процедура не Функция), 1 ⚠️ исправлено (имя метода `СведенияПоОКМТО` — опечатка в плане), 1 ⚠️ служебный с пометкой, 2 ⚠️ заменены / перекомпонованы (`СформироватьАдресИзXML` → `АдресПоИдентификатору`; `ПредставлениеКонтактнойИнформации` сигнатура уточнена + добавлен рекомендуемый `ПредставлениеКонтактнойИнформацииОбъекта`). Финальная таблица Key methods — 15 строк (14 стабильных + 1 ⚠️ служебный), в требуемом для L2 диапазоне 7–15. Метод `АдресныйКлассификатор.АдресныйКлассификаторЗагружен` сверен (строка 197), но в Key methods не вынесен отдельной строкой — упомянут в описании `ПроверитьАдреса` (для L2 таблица ограничена 15 строками).

## Self-check §9.1

| # | Пункт | Статус | Пояснение |
|---|---|---|---|
| 1 | Файл `.claude/skills/bsp-contact-info/SKILL.md` существует | ✅ | Создан |
| 2 | Frontmatter валиден: `name`, `description` (только триггеры, ≤ 1024 символа, начинается с `Use this skill when…`), `metadata.scope = bsp-3.1.11`, `metadata.version = "3.1.11"`, `metadata.layer = L2`; `metadata.related_subsystems` и `metadata.related_skills` заполнены | ✅ | description: 839 символов (≤ 1024 ✅), начинается с `Use this skill when the agent needs to…`, related_subsystems = [КонтактнаяИнформация, АдресныйКлассификатор], related_skills = [bsp-fundamentals, bsp-base-common, bsp-longs-and-jobs, bsp-print-reports] |
| 3 | `## When to use` содержит **минимум 3** типичных сценария (глагольная форма) | ✅ | 9 сценариев (чтение / запись КИ, представление, открытие формы, проверка, код региона, ОКТМО, ФИАС, детектор формата) |
| 4 | `## Не использовать, если` содержит **минимум 2** анти-триггера | ✅ | 6 анти-триггеров (bsp-base-common, bsp-print-reports, загрузка КЛАДР, фон для тяжёлой проверки, bsp-fundamentals, bsp-files-and-versions) |
| 5 | `## Core concepts` объясняет архитектуру подсистемы, имена общих модулей и карту «подсистема → модули» для затронутых подсистем | ✅ | Разделы: «Две подсистемы — один скил», «Внутренний формат хранения КИ — JSON», «Адресные форматы: КЛАДР vs ФИАС vs ОКТМО», три таблицы семейств модулей (КонтактнаяИнформация, АдресныйКлассификатор, РаботаСАдресами), «Тесная интеграция с другими подсистемами» |
| 6 | Таблица `## Key methods` содержит **7–15 строк** (для L2), с обязательными колонками `Метод \| Сигнатура \| Сервер/Клиент \| Назначение \| Пример вызова`. Для служебных модулей — отдельная колонка `Стабильность` с пометкой ⚠️. Пометка `(уточнить)` запрещена | ✅ | 15 строк (14 стабильных + 1 ⚠️ служебный с отдельной колонкой «Стабильность»); ни одной пометки `(уточнить)` (проверено grep) |
| 7 | Каждая строка `Key methods` дословно сверена по двум источникам (приоритет — `src/cf/`, дополнительно — `bsp3111_md/NNN_*.md`) | ✅ | см. `## Verification log` выше: 16 сверенных методов, номера строк указаны |
| 8 | `## Patterns` содержит **минимум 3** примера с кодом на BSL | ✅ | 6 паттернов (чтение КИ, представление, запись, открытие формы, фоновая проверка, разбор региона/ОКТМО) |
| 9 | `## Anti-patterns` содержит **минимум 5** пар «❌ / ✅» | ✅ | 7 пар (JSON в макете, ссылка vs объект, синхронный вызов проверки, ручной парсинг JSON, своё хранилище, КодРегионаПоНаименованию для городов, служебные модули без отметки) |
| 10 | `## How to explore deeper` для L2: имена модулей + ≥ 1 grep + ≥ 1 glob + (опционально) mermaid | ✅ | 5 разделов: 4 семейства модулей (14 имён), 6 grep-шаблонов, 6 glob-масок, mermaid-диаграмма подсистемы |
| 11 | В скиле **нет** ссылок на `bsp3111_md/`, `its.1c.ru`, `1c.ru`, `src/cf/`, и нет бинарных ассетов (PNG/JPEG/PDF) | ✅ | Проверено grep: 0 совпадений. Только имена модулей и скилов, никаких URL. Нет бинарных ассетов |
| 12 | Объём `SKILL.md` ≤ 500 строк / 5000 токенов; если превышает — Key methods/Patterns/Anti-patterns вынесены в `references/` | ✅ | 437 строк, 53 597 байт ≈ ~13 400 токенов (русский текст с кодом). Целевой объём не превышен, `references/` не требуется |
| 13 | Скил пройден self-review по чек-листу из `prompt-crafting-guide` | ✅ | Description: только триггеры, без workflow-summary, ≤ 1024 симв., без кода и сигнатур, на английском. Тело скила — на русском, BSL-примеры — на русском языке 1С. Mermaid-диаграмма (текст). Нет bullet-list'ов с эмодзи-эффектом, есть ⚠️ только для служебных методов по правилу §3.2 |

## Baseline log (RED-фаза, §9.0)

**Не выполнено** в этой итерации — пользователь явно указал «Use static verification. Do not run opencode run / smoke tests». Раздел зарезервирован для будущей итерации при подключении `opencode-runner`.

Ожидаемые 3 задачи из `## When to use` для будущего RED-прогона (для справки, не выполнялись):

1. «Напиши процедуру, которая по ссылке контрагента вернёт строку его юридического адреса для печатной формы. Используй подсистему БСП.»
2. «Как из строки JSON-адреса в базе извлечь регион (субъект РФ) и код КЛАДР?»
3. «Реализуй фоновую проверку пакета из 50 адресов контрагентов по веб-сервису 1С.»

## Smoke-test log (GREEN-фаза, §9.2 / §9.4)

**Не выполнено** в этой итерации — пользователь явно указал «Use static verification. Do not run opencode run / smoke tests». Раздел зарезервирован для будущей итерации.

## REFACTOR log

**Не выполнено** в этой итерации (нет RED/GREEN-прогонов). В текущей итерации выполнена только статическая верификация сигнатур и структуры скила. Выявленные на этапе сверки и **закрытые** в этом коммите (без отдельного REFACTOR-цикла) несоответствия плану §6.1:

1. **Сигнатура `ЗаписатьКонтактнуюИнформацию`** — план не указал явно, что это **Процедура**, а не Функция. В скиле Key methods помечено: «**Процедура** (не Функция)».
2. **Имя метода `СведенияПоОКМТО`** — план содержит опечатку `…ОКМТО` (из `bsp3111_md/016_…md` имени файла), реальное имя в исходниках и в официальной документации метода — `СведенияПоОКМТО` (с буквой `Т` перед `М`). В скиле использовано правильное имя.
3. **Сигнатура `ПредставлениеКонтактнойИнформации`** — план указал `(ЗначениеПолей, ВидИнформации)`, реальная — `(Знач КонтактнаяИнформация, Знач ПеревестиВТранслит = Ложь)`. В скиле сигнатура исправлена + добавлен **рекомендуемый** метод `ПредставлениеКонтактнойИнформацииОбъекта` (прямая рекомендация из `bsp3111_md/1181_…md`).
4. **Метод `РаботаСАдресами.СформироватьАдресИзXML`** — план указал «(уточнить)», и этого метода **не существует** в `src/cf/CommonModules/РаботаСАдресами/Ext/Module.bsl`. В скиле заменён на `РаботаСАдресами.АдресПоИдентификатору` (ближайший аналог, ФИАС-сборка), плюс добавлен `СведенияОбАдресе` как основной метод разбора.
5. **Сигнатура `ОткрытьФормуКонтактнойИнформации`** — план указал «(уточнить)», реальная — `(Параметры, Владелец = Неопределено, Оповещение = Неопределено)`. В скиле сигнатура полная.
6. **Семейство `УправлениеКонтактнойИнформациейСлужебныйКлиент`** / `…СлужебныйКлиентСервер` — **не существуют** (только серверные служебные варианты). В скиле в Core concepts явно указано.
7. **Семейство `РаботаСАдресамиСлужебный`** (без суффикса) — **не существует** (есть только `…СлужебныйКлиент`). В скиле в Core concepts явно указано.
