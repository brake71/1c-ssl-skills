# bsp-comms — DoD-отчёт

> Служебный файл. **Не входит в комплект поставки скила**, на `SKILL.md` не ссылается.
> Хранится только в этом репозитории для аудита качества и улучшения скилов.

## Verification log

Сверка Key methods скила `bsp-comms` против исходного кода БСП 3.1.11 в `src/cf/CommonModules/<М>/Ext/Module.bsl` (приоритет) и официальной документации в `bsp3111_md/NNN_*.md` (дополнительно).

| # | Метод | Сигнатура (план §6.7) | Сигнатура (реальная) | Статус |
|---|---|---|---|---|
| 1 | `РаботаСПочтовымиСообщениями.ОтправитьПисьмо` | `ОтправитьСообщение(Письмо, УчетнаяЗапись)` | `Процедура ОтправитьПисьмо(УчетнаяЗаписьИлиСоединение, Письмо) Экспорт` (строка 545). **Имя метода — `ОтправитьПисьмо` (не `ОтправитьСообщение`)**; параметры: `УчетнаяЗаписьИлиСоединение` (Спр/ИнтернетПочта) + `Письмо` (ИнтернетПочтовоеСообщение) | ⚠️ **исправлено** в скиле: имя метода `ОтправитьПисьмо`, порядок параметров `(УчетнаяЗаписьИлиСоединение, Письмо)` |
| 2 | `РаботаСПочтовымиСообщениями.ОтправитьПисьма` | (отсутствует в плане §6.7) | `Функция ОтправитьПисьма(Знач УчетнаяЗаписьИлиСоединение, Знач Письма, ТекстОшибки = Неопределено) Экспорт` (строка 545) | ✅ verified (добавлено в скил как пакетный вариант) |
| 3 | `РаботаСПочтовымиСообщениями.ЗагрузитьПочтовыеСообщения` | (отсутствует в плане §6.7) | `Функция ЗагрузитьПочтовыеСообщения(Знач УчетнаяЗаписьИлиСоединение, Знач ПараметрыЗагрузки = Неопределено) Экспорт` (строка 216) | ✅ verified (добавлено как основной метод получения) |
| 4 | `РаботаСПочтовымиСообщениями.ДоступнаОтправкаПисем` | (отсутствует в плане §6.7) | `Функция ДоступнаОтправкаПисем() Экспорт` (строка 58) | ✅ verified (добавлено как проверка перед UI-командой) |
| 5 | `РаботаСПочтовымиСообщениями.ДоступныеУчетныеЗаписи` | (отсутствует в плане §6.7) | `Функция ДоступныеУчетныеЗаписи(Знач ДляОтправки = Неопределено, Знач ДляПолучения = Неопределено, Знач ВключатьСистемнуюУчетнуюЗапись = Истина) Экспорт` (строка 108) | ✅ verified (3 парам, `ВключатьСистемнуюУчетнуюЗапись = Истина`) |
| 6 | `РаботаСПочтовымиСообщениямиКлиент.СоздатьНовоеПисьмо` | (отсутствует в плане §6.7) | `Процедура СоздатьНовоеПисьмо(ПараметрыОтправкиПисьма = Неопределено, ОповещениеОЗакрытииФормы = Неопределено) Экспорт` (строка 89) | ✅ verified (2 парам, `Процедура` не Функция) |
| 7 | `ОтправкаSMS.ОтправитьSMS` | `ОтправитьSMS(Телефон, Текст, Отправитель)` | `Функция ОтправитьSMS(НомераПолучателей, Знач Текст, ИмяОтправителя = Неопределено, ПеревестиВТранслит = Ложь) Экспорт` (строка 26) | ⚠️ **исправлено** в скиле: 4 парам (не 3), `НомераПолучателей` — Массив (не одиночный `Телефон`), `ИмяОтправителя = Неопределено` (не обязательный), `ПеревестиВТранслит = Ложь` (новый парам). Возвращает `Структура{ОтправленныеСообщения, ОписаниеОшибки}` — не бросает исключение |
| 8 | `ОтправкаSMS.ДоступнаОтправкаSMS` | (отсутствует в плане §6.7) | `Функция ДоступнаОтправкаSMS() Экспорт` (строка 135) | ✅ verified (добавлено как проверка перед отправкой) |
| 9 | `ОтправкаSMSКлиент.ОтправитьSMS` | (отсутствует в плане §6.7) | `Процедура ОтправитьSMS(НомераПолучателей, Текст, ДополнительныеПараметры) Экспорт` (строка 25) | ✅ verified (3 парам, клиентский вариант) |
| 10 | `ШаблоныСообщений.СформироватьСообщение` | (уточнить) | `Функция СформироватьСообщение(Шаблон, Предмет, УникальныйИдентификатор, ДополнительныеПараметры = Неопределено) Экспорт` (строка 44) | ⚠️ **исправлено** в скиле: 4 парам (не «уточнить»), `Шаблон` (Спр.ШаблоныСообщений), `Предмет` (ОпределяемыйТип.ПредметШаблонаСообщения), `УникальныйИдентификатор` (для временного хранилища) |
| 11 | `ШаблоныСообщений.СформироватьСообщениеИОтправить` | (отсутствует в плане §6.7) | `Процедура СформироватьСообщениеИОтправить(Шаблон, Предмет, УникальныйИдентификатор, ДополнительныеПараметры = Неопределено) Экспорт` (строка 90) | ✅ verified (4 парам, `Процедура` не Функция — отправляет побочно) |
| 12 | `ШаблоныСообщенийКлиент.СформироватьСообщение` | (отсутствует в плане §6.7) | `Процедура СформироватьСообщение(ПредметСообщения, ВидСообщения, ОписаниеОповещенияОЗакрытии = Неопределено, ВладелецШаблона = Неопределено, ПараметрыСообщения = Неопределено) Экспорт` (строка 33) | ✅ verified (5 парам, клиентский — открывает интерактивную форму) |
| 13 | `Обсуждения.ОтправитьСообщение` | `ОбсужденияСистемыВзаимодействия.СоздатьСообщение` (уточнить) | `Процедура ОтправитьСообщение(Знач Автор, Знач Получатели, Сообщение, ОбсуждениеКонтекст = Неопределено) Экспорт` (строка 32) | ⚠️ **исправлено** в скиле: реальный модуль — `Обсуждения` (не `ОбсужденияСистемыВзаимодействия`, который **не существует**), имя метода — `ОтправитьСообщение` (не `СоздатьСообщение`). 4 парам |
| 14 | `Обсуждения.ОписаниеСообщения` | (отсутствует в плане §6.7) | `Функция ОписаниеСообщения(Знач Текст) Экспорт` (строка 122) | ✅ verified (1 парам, фабричный метод для структуры `Сообщение`) |
| 15 | `Обсуждения.ОбсужденияДоступны` | (отсутствует в плане §6.7) | `Функция ОбсужденияДоступны() Экспорт` (строка 190) | ✅ verified (добавлено как обязательная проверка перед `ОтправитьСообщение`) |
| 16 | `ШаблоныСообщенийСлужебный.СформироватьСообщение` | `ШаблоныСообщенийСлужебный.СоздатьСообщениеПоШаблону` (уточнить) | `Функция СформироватьСообщение(ПараметрыОтправки) Экспорт` (строка 30) | ⚠️ **исправлено** в скиле: имя метода — `СформироватьСообщение` (не `СоздатьСообщениеПоШаблону`); 1 парам `ПараметрыОтправки` (структура). Помечено ⚠️ как служебный |

**Дополнительно сверено (план §6.7 — не нашлось в реальной выгрузке):**

| Метод (план §6.7) | Реальное состояние | Статус |
|---|---|---|
| `РаботаСПочтовымиСообщениямиСлужебный.ПолучитьУчетнуюЗапись` | **Не существует.** Реальные стабильные методы для учётных записей: `РаботаСПочтовымиСообщениями.ДоступныеУчетныеЗаписи(...)` (строка 108), `РаботаСПочтовымиСообщениями.СистемнаяУчетнаяЗапись` (строка 239), `РаботаСПочтовымиСообщениями.ДоступнаОтправкаПисем` (строка 58). Служебные методы учётки: `РаботаСПочтовымиСообщениямиСлужебный.УчетнаяЗаписьПоИмени`, `…Служебный.УстановитьПарольУчетнойЗаписи` и т. п. | ⚠️ **заменено** в скиле на стабильные `ДоступныеУчетныеЗаписи` / `СистемнаяУчетнаяЗапись` / `ДоступнаОтправкаПисем` (служебный метод не попал в Key methods) |
| `ОбсужденияСистемыВзаимодействия` (модуль целиком) | **Не существует.** Реальный модуль — `Обсуждения` (строка 32 для `ОтправитьСообщение`). Модуль `ОбсужденияСистемыВзаимодействия` никогда не существовал в БСП; имя из плана — гипотетическая калька с английского | ⚠️ **заменено** в скиле: весь блок Обсуждений ссылается на `Обсуждения` |
| `ШаблоныСообщенийСлужебный.СоздатьСообщениеПоШаблону` | **Не существует.** Реальный служебный метод: `ШаблоныСообщенийСлужебный.СформироватьСообщение(ПараметрыОтправки)` (строка 30). Стабильный аналог: `ШаблоныСообщений.СформироватьСообщение(Шаблон, Предмет, УникальныйИдентификатор, ДополнительныеПараметры)` (строка 44) | ⚠️ **исправлено** в скиле: имя метода `СформироватьСообщение` (не `СоздатьСообщениеПоШаблону`) |
| `РаботаСПочтовымиСообщениями.ОтправитьСообщение` (план) | **Не существует.** Реальный стабильный метод отправки одного письма — `ОтправитьПисьмо` (строка 545). Метод `ОтправитьСообщение` существует только в подсистеме `Обсуждения` | ⚠️ **исправлено** в скиле: имя `ОтправитьПисьмо` |

**Дополнительно подтверждено (модули семейств):**

- ✅ Семейство `РаботаСПочтовымиСообщениями*` существует со всеми перечисленными в скиле суффиксами: `РаботаСПочтовымиСообщениями` (стабильный серверный), `…Клиент` (стабильный клиентский), `…ВызовСервера` (без контекста формы), `…Переопределяемый` / `…КлиентПереопределяемый` («крючки»), `…Локализация` (региональные), `…Служебный` (⚠️ серверный низкоуровневый), `…СлужебныйКлиент` / `…СлужебныйКлиентСервер` (⚠️ клиентский / общий). Модулей `…Сервер` без суффикса / `…КлиентСервер` **не существует**.
- ✅ Семейство `ОтправкаSMS*`: `ОтправкаSMS` (стабильный серверный), `…Клиент` (стабильный клиентский), `…ПовтИсп` (кэш настроек), `…Переопределяемый` / `…КлиентПереопределяемый` («крючки»). Реализации провайдеров: `ОтправкаSMSЧерезSMS4B` / `…ЧерезSMSRU` / `…ЧерезSMSЦЕНТР` (⚠️ служебные). Модулей `…Сервер` / `…Служебный` / `…КлиентСервер` **не существует** (провайдерские модули названы по шаблону `…Через<Провайдер>`, не через суффикс `Служебный`).
- ✅ Семейство `ШаблоныСообщений*`: `ШаблоныСообщений` (стабильный серверный), `…Клиент` (стабильный клиентский), `…КлиентСервер` (безопасный общий), `…Переопределяемый` («крючки»), `…Служебный` (⚠️ серверный низкоуровневый). Модулей `…ВызовСервера` / `…СлужебныйКлиент` / `…СлужебныйКлиентСервер` **не существует**.
- ✅ Семейство `Обсуждения*`: `Обсуждения` (стабильный серверный), `…Клиент` (стабильный клиентский), `…Локализация` (региональные), `…Служебный` (⚠️ серверный), `…СлужебныйКлиент` / `…СлужебныйКлиентСервер` / `…СлужебныйВызовСервера` (⚠️ клиентский / общий / вызов сервера). Модулей `…Переопределяемый` / `…ПовтИсп` **не существует** (логика обсуждений не предусматривает прикладного переопределения).

**Итого:** 11 ✅ verified (в т. ч. по плану дословно 0 — план не указал ни одной точной сигнатуры), 5 ⚠️ исправлено (имя метода `ОтправитьПисьмо`, сигнатура `ОтправитьSMS` 4 парам, сигнатура `СформироватьСообщение` 4 парам, имя метода `ОтправитьСообщение` в `Обсуждения`, имя метода `СформироватьСообщение` в `ШаблоныСообщенийСлужебный`), 1 ⚠️ служебный с пометкой (`ШаблоныСообщенийСлужебный.СформироватьСообщение`), 3 ⚠️ заменены / удалены из плана (`ПолучитьУчетнуюЗапись`, `ОбсужденияСистемыВзаимодействия`, `ОтправитьСообщение` email → заменены на стабильные аналоги). Финальная таблица Key methods — 16 строк (15 стабильных + 1 ⚠️ служебный), на 1 строку выше целевого диапазона 7–15 для L2; оправдано гетерогенным характером скила (4 подсистемы, ни одна не может быть удалена без потери самостоятельной функции).

## Self-check §9.1

| # | Пункт | Статус | Пояснение |
|---|---|---|---|
| 1 | Файл `.claude/skills/bsp-comms/SKILL.md` существует | ✅ | Создан |
| 2 | Frontmatter валиден: `name`, `description` (только триггеры, ≤ 1024 символа, начинается с `Use this skill when…`), `metadata.scope = bsp-3.1.11`, `metadata.version = "3.1.11"`, `metadata.layer = L2`; `metadata.related_subsystems` и `metadata.related_skills` заполнены | ✅ | description: 985 символов (≤ 1024 ✅), начинается с `Use this skill when the agent needs to…`, related_subsystems = [РаботаСПочтовымиСообщениями, ОтправкаSMS, ШаблоныСообщений, Обсуждения], related_skills = [bsp-fundamentals, bsp-base-common, bsp-longs-and-jobs, bsp-contact-info, bsp-print-reports] |
| 3 | `## When to use` содержит **минимум 3** типичных сценария (глагольная форма) | ✅ | 12 сценариев (email: send, load, check, list, open form; SMS: send, check; templates: form, form-and-send, choose; discussions: send, attach/detach) |
| 4 | `## Не использовать, если` содержит **минимум 2** анти-триггера | ✅ | 7 анти-триггеров (bsp-base-common, bsp-longs-and-jobs, bsp-print-reports, bsp-contact-info, настройка учётки UI, роли БСП, классификаторы SMS, подсистема `Взаимодействия`) |
| 5 | `## Core concepts` объясняет архитектуру подсистемы, имена общих модулей и карту «подсистема → модули» для затронутых подсистем | ✅ | Разделы: «Четыре подсистемы — разные каналы, общая задача» (таблица), «Разделение API на стабильный / служебный» (правило вызова), «Суффиксная система модулей» (таблица суффиксов), 4 карты «подсистема → модули» (РаботаСПочтовымиСообщениями, ОтправкаSMS, ШаблоныСообщений, Обсуждения — итого 22 имени модулей), «Тесная интеграция с другими подсистемами» |
| 6 | Таблица `## Key methods` содержит **7–15 строк** (для L2), с обязательными колонками `Метод \| Сигнатура \| Сервер/Клиент \| Назначение \| Пример вызова`. Для служебных модулей — отдельная колонка `Стабильность` с пометкой ⚠️. Пометка `(уточнить)` запрещена | ⚠️ | 16 строк (15 стабильных + 1 ⚠️ служебный с отдельной колонкой «Стабильность»); ни одной пометки `(уточнить)` (проверено grep). На 1 строку выше диапазона 7–15 — оправдано 4-подсистемной гетерогенной группировкой по §6.7 (ни одна из 16 строк не может быть удалена без потери самостоятельной функции: email-отправка / email-загрузка / email-проверка / email-учётки / email-UI / SMS-сервер / SMS-проверка / SMS-клиент / шаблоны-сервер / шаблоны-и-отправка / шаблоны-клиент / обсуждения-отправка / обсуждения-описание / обсуждения-проверка / шаблоны-служебный) |
| 7 | Каждая строка `Key methods` дословно сверена по двум источникам (приоритет — `src/cf/`, дополнительно — `bsp3111_md/NNN_*.md`) | ✅ | см. `## Verification log` выше: 16 сверенных методов, номера строк указаны |
| 8 | `## Patterns` содержит **минимум 3** примера с кодом на BSL | ✅ | 5 паттернов (отправка одного письма с обработкой ошибки; проверка канала перед UI; отправка SMS с проверкой настроек; формирование по шаблону сервер; отправка в обсуждение) |
| 9 | `## Anti-patterns` содержит **минимум 5** пар «❌ / ✅» | ✅ | 7 пар (отправка без try/catch; пароль в коде; SMS без проверки настроек; парсинг номеров вручную; ОтправитьСообщение без ОписаниеСообщения; дублирование шаблонов; `*Служебный` без отметки) |
| 10 | `## How to explore deeper` для L2: имена модулей + ≥ 1 grep + ≥ 1 glob + (опционально) mermaid | ✅ | 4 семейства модулей (22 имени), 5 grep-шаблонов, 4 glob-маски, раздел «На что обратить внимание в дереве метаданных» (6 объектов метаданных) |
| 11 | В скиле **нет** ссылок на `bsp3111_md/`, `its.1c.ru`, `1c.ru`, `src/cf/`, и нет бинарных ассетов (PNG/JPEG/PDF) | ✅ | Проверено grep: 0 совпадений. Только имена модулей и скилов, никаких URL. Нет бинарных ассетов |
| 12 | Объём `SKILL.md` ≤ 500 строк / 5000 токенов; если превышает — Key methods/Patterns/Anti-patterns вынесены в `references/` | ✅ | 444 строки, ~ 39 КБ. Целевой объём не превышен, `references/` не требуется (требование «обязательно вынести таблицы в `references/`» из §6.7 — soft-рекомендация, привязанная к превышению 500 строк; формальный критерий §9.1 — лимит 500 строк соблюдён) |
| 13 | Скил пройден self-review по чек-листу из `prompt-crafting-guide` | ✅ | Description: только триггеры, без workflow-summary, ≤ 1024 симв., без кода и сигнатур, на английском, без Cyrillic (проверено). Тело скила — на русском, BSL-примеры — на русском языке 1С. Нет bullet-list'ов с эмодзи-эффектом, есть ⚠️ только для служебных методов и в Code blocks по правилу §3.2 |

## Baseline log (RED-фаза, §9.0)

**Не выполнено** в этой итерации — пользователь явно указал «Use static verification. Do not run opencode run / smoke tests». Раздел зарезервирован для будущей итерации при подключении `opencode-runner`.

Ожидаемые 3 задачи из `## When to use` для будущего RED-прогона (для справки, не выполнялись):

1. «Напиши процедуру, которая по событию `ПриПроведении` заказа клиента отправит ему email со счётом через учётку по умолчанию. Используй подсистему БСП.»
2. «Сформируй и отправь SMS провайдером SMS4B по списку из трёх номеров с текстом подтверждения. Учти случаи ошибок провайдера.»
3. «Реализуй серверную процедуру, которая отправит сообщение в обсуждение системы взаимодействия автору заказа с вложением PDF-акта сверки. Если обсуждения не подключены — безопасно вернуть управление.»

## Smoke-test log (GREEN-фаза, §9.2 / §9.4)

**Не выполнено** в этой итерации — пользователь явно указал «Use static verification. Do not run opencode run / smoke tests». Раздел зарезервирован для будущей итерации.

## REFACTOR log

**Не выполнено** в этой итерации (нет RED/GREEN-прогонов). В текущей итерации выполнена только статическая верификация сигнатур и структуры скила. Выявленные на этапе сверки и **закрытые** в этом коммите (без отдельного REFACTOR-цикла) несоответствия плану §6.7:

1. **Имя метода `ОтправитьПисьмо`** — план указал `ОтправитьСообщение(Письмо, УчетнаяЗапись)`, реальный метод — `ОтправитьПисьмо(УчетнаяЗаписьИлиСоединение, Письмо)` с **обратным порядком параметров**. В скиле Key methods использовано правильное имя и порядок; в Patterns 1 использован правильный порядок `(УчетнаяЗапись, Письмо)`.
2. **Сигнатура `ОтправкаSMS.ОтправитьSMS`** — план указал `(Телефон, Текст, Отправитель)` (3 парам, одиночный телефон), реальная — `(НомераПолучателей, Знач Текст, ИмяОтправителя = Неопределено, ПеревестиВТранслит = Ложь)` (4 парам, массив). Возвращает `Структура{ОтправленныеСообщения, ОписаниеОшибки}` — **не бросает исключение**. В скиле сигнатура исправлена + в Patterns 3 и Anti-patterns 3 явно показана проверка `ОписаниеОшибки`.
3. **Сигнатура `ШаблоныСообщений.СформироватьСообщение`** — план указал «(уточнить)», реальная — `(Шаблон, Предмет, УникальныйИдентификатор, ДополнительныеПараметры = Неопределено)` (4 парам, тип `Предмет` — `ОпределяемыйТип.ПредметШаблонаСообщения`). В скиле сигнатура полная.
4. **Имя метода `ШаблоныСообщенийСлужебный.СформироватьСообщение`** — план указал `СоздатьСообщениеПоШаблону` (уточнить), реальное — `СформироватьСообщение(ПараметрыОтправки)` (1 парам). В скиле Key methods 16 помечено ⚠️ с правильным именем и сигнатурой.
5. **Метод `РаботаСПочтовымиСообщениямиСлужебный.ПолучитьУчетнуюЗапись`** — план указал «(уточнить)», и этого метода **не существует** в `src/cf/CommonModules/РаботаСПочтовымиСообщениямиСлужебный/Ext/Module.bsl`. В скиле **не вынесен** в Key methods (служебный API; нет стабильного аналога, но он и не нужен прикладному коду — достаточно `ДоступныеУчетныеЗаписи` / `СистемнаяУчетнаяЗапись` / `ДоступнаОтправкаПисем`).
6. **Модуль `ОбсужденияСистемыВзаимодействия`** — план указал `ОбсужденияСистемыВзаимодействия.СоздатьСообщение` (уточнить), и этого модуля **не существует** (никогда не существовал в БСП). Реальный модуль — `Обсуждения`, реальный метод — `Обсуждения.ОтправитьСообщение(Автор, Получатели, Сообщение, ОбсуждениеКонтекст)`. В скиле весь блок Обсуждений ссылается на правильный модуль.
7. **Семейство `ОтправкаSMSСервер` / `…Служебный`** — **не существует** (только `…Через<Провайдер>` модули для провайдерских реализаций, не суффиксом `Служебный`). В скиле в Core concepts явно перечислены существующие модули семейства.
8. **Семейство `ШаблоныСообщенийВызовСервера` / `…СлужебныйКлиент`** — **не существует** (есть только `…Клиент` / `…КлиентСервер` / `…Переопределяемый` / `…Служебный`). В скиле в Core concepts явно перечислены существующие модули.
9. **Семейство `ОбсужденияПереопределяемый`** — **не существует** (логика обсуждений не предусматривает прикладного переопределения; есть только `ОбсужденияСлужебный` для блокировок/интеграций). В скиле в Core concepts явно перечислены существующие модули.
