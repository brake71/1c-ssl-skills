# bsp-perf-monitoring — DoD-отчёт

> Служебный файл. **Не входит в комплект поставки скила**, на `SKILL.md` не ссылается.
> Хранится только в этом репозитории для аудита качества и улучшения скилов.

## Verification log

Каждая строка `## Key methods` сверена по приоритету `src/cf/CommonModules/<…>/Ext/Module.bsl` (для подтверждения сигнатуры и существования) и `bsp3111_md/<NNN_*.md>` (для сценариев использования). Записи — в формате `<!-- verified: <путь>:<строка> -->`.

| Метод | Источник-победитель | Запись сверки |
|---|---|---|
| `ОценкаПроизводительности.НачатьЗамерВремени` | `src/cf/CommonModules/ОценкаПроизводительности/Ext/Module.bsl:17` (фактическая сигнатура `() → Число`) | `<!-- verified: src/cf/CommonModules/ОценкаПроизводительности/Ext/Module.bsl:17 -->` |
| `ОценкаПроизводительности.ЗакончитьЗамерВремени` | `src/cf/CommonModules/ОценкаПроизводительности/Ext/Module.bsl:42` + `bsp3111_md/1962_закончитьзамервремени.md` (сигнатура совпадает: 5 параметров с дефолтами) | `<!-- verified: src/cf/CommonModules/ОценкаПроизводительности/Ext/Module.bsl:42 -->` |
| `ОценкаПроизводительности.НачатьЗамерДлительнойОперации` | `src/cf/CommonModules/ОценкаПроизводительности/Ext/Module.bsl:307` + `bsp3111_md/1968_начатьзамердлительнойоперации.md` | `<!-- verified: src/cf/CommonModules/ОценкаПроизводительности/Ext/Module.bsl:307 -->` |
| `ОценкаПроизводительности.ЗафиксироватьЗамерДлительнойОперации` | `src/cf/CommonModules/ОценкаПроизводительности/Ext/Module.bsl:330` + `bsp3111_md/1974_зафиксироватьзамердлительнойоперации.md` | `<!-- verified: src/cf/CommonModules/ОценкаПроизводительности/Ext/Module.bsl:330 -->` |
| `ОценкаПроизводительности.ЗакончитьЗамерДлительнойОперации` | `src/cf/CommonModules/ОценкаПроизводительности/Ext/Module.bsl:370` + `bsp3111_md/1964_закончитьзамердлительнойоперации.md` | `<!-- verified: src/cf/CommonModules/ОценкаПроизводительности/Ext/Module.bsl:370 -->` |
| `ОценкаПроизводительности.СоздатьКлючевуюОперацию` | `src/cf/CommonModules/ОценкаПроизводительности/Ext/Module.bsl:541` (сигнатура 3 параметра с дефолтами, возврат `СправочникСсылка.КлючевыеОперации`) | `<!-- verified: src/cf/CommonModules/ОценкаПроизводительности/Ext/Module.bsl:541 -->` |
| `ОценкаПроизводительностиКлиент.ЗамерВремени` | `src/cf/CommonModules/ОценкаПроизводительностиКлиент/Ext/Module.bsl:37` (сигнатура с 3 параметрами и `АвтоЗавершение = Истина`) | `<!-- verified: src/cf/CommonModules/ОценкаПроизводительностиКлиент/Ext/Module.bsl:37 -->` |
| `ОценкаПроизводительностиКлиент.НачатьЗамерВремени` | `src/cf/CommonModules/ОценкаПроизводительностиКлиент/Ext/Module.bsl:392` (клиентский вариант; пара с `ЗавершитьЗамерВремени`) | `<!-- verified: src/cf/CommonModules/ОценкаПроизводительностиКлиент/Ext/Module.bsl:392 -->` |
| `ОценкаПроизводительностиКлиент.ЗавершитьЗамерВремени` | `src/cf/CommonModules/ОценкаПроизводительностиКлиент/Ext/Module.bsl:91` (принимает `УИДЗамера`, не `ОписаниеЗамера`) | `<!-- verified: src/cf/CommonModules/ОценкаПроизводительностиКлиент/Ext/Module.bsl:91 -->` |
| `ЦентрМониторинга.ЦентрМониторингаВключен` | `src/cf/CommonModules/ЦентрМониторинга/Ext/Module.bsl:17` + `bsp3111_md/2756_центрмониторингавключен.md` | `<!-- verified: src/cf/CommonModules/ЦентрМониторинга/Ext/Module.bsl:17 -->` |
| `ЦентрМониторинга.ЗаписатьОперациюБизнесСтатистики` | `src/cf/CommonModules/ЦентрМониторинга/Ext/Module.bsl:96` + `bsp3111_md/2748_записатьоперациюбизнесстатистики.md` | `<!-- verified: src/cf/CommonModules/ЦентрМониторинга/Ext/Module.bsl:96 -->` |
| `ЦентрМониторинга.ЗаписатьОперациюБизнесСтатистикиЧас` | `src/cf/CommonModules/ЦентрМониторинга/Ext/Module.bsl:115` + `bsp3111_md/2750_записатьоперациюбизнесстатистикичас.md` | `<!-- verified: src/cf/CommonModules/ЦентрМониторинга/Ext/Module.bsl:115 -->` |
| `ЦентрМониторинга.ЗаписатьОперациюБизнесСтатистикиСутки` | `src/cf/CommonModules/ЦентрМониторинга/Ext/Module.bsl:141` (сигнатура совпадает с часовой) | `<!-- verified: src/cf/CommonModules/ЦентрМониторинга/Ext/Module.bsl:141 -->` |
| ⚠️ `ЦентрМониторингаСлужебный.ЗаписатьОперациюБизнесСтатистикиСлужебная` | `src/cf/CommonModules/ЦентрМониторингаСлужебный/Ext/Module.bsl:3020` (⚠️ служебный модуль; метод помечен в Key methods с предупреждением) | `<!-- verified: src/cf/CommonModules/ЦентрМониторингаСлужебный/Ext/Module.bsl:3020 -->` |

### Расхождения плана §6.6 с реальными именами/сигнатурами

| §6.6 план (Key methods) | Реальность | Что сделано |
|---|---|---|
| `ОценкаПроизводительности.НачатьЗамерВремени(КлючеваяОперация)` | Фактически `НачатьЗамерВремени()` — **без параметров**, возвращает `Число` (UTC мс). Имя ключевой операции передаётся в `ЗакончитьЗамерВремени` | В Key methods сигнатура исправлена на безаргументную с пояснением, что имя передаётся при завершении |
| `ОценкаПроизводительности.ЗакончитьЗамерВремени` (помечено «(уточнить)») | Фактически `ЗакончитьЗамерВремени(КлючеваяОперация, ВремяНачала, ВесЗамера=1, Комментарий=Неопределено, ВыполненСОшибкой=Ложь)` | Полная сигнатура с дефолтами |
| `ОценкаПроизводительности.ЗафиксироватьДлительностьКлючевойОперации` (помечено «(уточнить)») | **Метода с таким именем НЕ СУЩЕСТВУЕТ.** Реальные аналоги: `ЗакончитьЗамерВремени` (одиночная запись) и `ЗафиксироватьЗамерДлительнойОперации` (вложенный шаг длительной операции) | В Key methods добавлен `ЗакончитьЗамерДлительнойОперации` + `ЗафиксироватьЗамерДлительнойОперации`; в Anti-patterns — явный ❌ на выдуманное имя + ссылка на правильный путь |
| `ЦентрМониторинга.ЗаписатьОперацию` (помечено «(уточнить)») | Фактически `ЗаписатьОперациюБизнесСтатистики` (полное имя), с опциональными `Комментарий` и `Разделитель` | Полное имя в Key methods; в Anti-patterns — ❌ на сокращённое |
| `ЦентрМониторингаСлужебный.ЗаписатьСобытие` (помечено «(уточнить)») | Фактически `ЗаписатьОперациюБизнесСтатистикиСлужебная(ПараметрыЗаписи)` | Полное имя в Key methods с ⚠️ |

### Дополнения сверх плана §6.6 (L2 норма — 7–15 строк, в плане было 5)

План §6.6 дал 5 «(уточнить)»-методов. Реальный Key methods скила — **14 строк** (включая ⚠️), что укладывается в L2-диапазон 7–15. Дополнительно добавлены:

- `ОценкаПроизводительности.НачатьЗамерДлительнойОперации` + `ЗакончитьЗамерДлительнойОперации` + `ЗафиксироватьЗамерДлительнойОперации` — семейство длительных операций (в Core concepts явно отмечено отличие от подсистемы «Длительные операции»).
- `ОценкаПроизводительности.СоздатьКлючевуюОперацию` — создание записи справочника (часто нужно при добавлении новой ключевой операции в прикладном коде).
- `ОценкаПроизводительностиКлиент.ЗамерВремени` + `НачатьЗамерВремени` + `ЗавершитьЗамерВремени` — клиентское семейство (важная часть подсистемы, без него нельзя замерить открытие формы).
- ⚠️ `ЦентрМониторингаСлужебный.ЗаписатьОперациюБизнесСтатистикиСлужебная` — служебный аналог стабильных методов, помечен ⚠️.

Дополнения не противоречат §6.6: в нём перечислен **минимум**, а не исчерпывающий список. L2-диапазон 7–15 строк соблюдён (14 строк).

### Сверка семейств модулей (для Core concepts)

- `CommonModules/ОценкаПроизводительности` (Server, ExternalConnection, ClientOrdinary — `<Server>true</Server>`, `<ClientManagedApplication>false</ClientManagedApplication>`) — `src/cf/CommonModules/ОценкаПроизводительности.xml`.
- `CommonModules/ОценкаПроизводительностиКлиент` (Client only) — `src/cf/CommonModules/ОценкаПроизводительностиКлиент.xml`.
- `CommonModules/ОценкаПроизводительностиКлиентСервер` (Client + Server + External) — `src/cf/CommonModules/ОценкаПроизводительностиКлиентСервер.xml`.
- `CommonModules/ОценкаПроизводительностиВызовСервера` (Server) — содержит `ЗафиксироватьДлительностьКлючевыхОпераций` (низкоуровневая пакетная запись, упомянута в Core concepts).
- `CommonModules/ОценкаПроизводительностиВызовСервераПовтИсп` (Server ПовтИсп) — кешированный гейт `ВыполнятьЗамерыПроизводительности()`.
- `CommonModules/ОценкаПроизводительностиГлобальный` (Global) — служебные авто-обработчики.
- ⚠️ `CommonModules/ОценкаПроизводительностиСлужебный` (Server) — служебный.
- `CommonModules/ЦентрМониторинга` (Server, ExternalConnection, ClientOrdinary) — стабильный API.
- `CommonModules/ЦентрМониторингаКлиент` (Client only) — UI и облегчённые методы.
- ⚠️ `CommonModules/ЦентрМониторингаСлужебный` (Server) — служебный, `ЗаписатьОперациюБизнесСтатистикиСлужебная` (line 3020) + регламентные задания.
- `CommonModules/ЦентрМониторингаГлобальный`, `*Переопределяемый`, `*ПовтИсп`, `*КлиентСлужебный` — вспомогательные.

## Self-check §9.1

Все 13 пунктов из §9.1:

1. ✅ Файл `.claude/skills/bsp-perf-monitoring/SKILL.md` существует (417 строк ≤ 500).
2. ✅ Frontmatter валиден: `name: bsp-perf-monitoring`, `description` — только триггеры, 540 символов, начинается с `Use this skill when…`; `metadata.scope = bsp-3.1.11`, `metadata.version = "3.1.11"`, `metadata.layer = L2`; `related_subsystems = [ОценкаПроизводительности, ЦентрМониторинга]`; `related_skills = [bsp-fundamentals, bsp-base-common]`. Проверено программно: описание не содержит кириллицы.
3. ✅ `## When to use` содержит 6 триггеров (нужно обернуть замером APDEX; замерить сложную операцию с вложенными шагами; зарегистрировать бизнес-статистику; проверить, включён ли центр мониторинга; программно включить/отключить подсистему; программно создать ключевую операцию). Минимум 3 — выполнено.
4. ✅ `## Не использовать, если` содержит 5 анти-триггеров (выполнить длительную операцию в фоне; планировать регламентное задание; отправить отчёт об ошибке; измерить произвольный интервал без записи; запись в журнал регистрации). Минимум 2 — выполнено.
5. ✅ `## Core concepts` объясняет архитектуру обеих подсистем, семейства модулей с указанием контекста (Server/Client/ClientServer/⚠️Служебный), и карту «подсистема → модули» (отдельные таблицы для «ОценкаПроизводительности» и «ЦентрМониторинга»).
6. ✅ Таблица `## Key methods` содержит **14 строк** (L2 диапазон 7–15), с обязательными колонками `Метод | Сигнатура | Сервер/Клиент | Назначение | Стабильность | Пример вызова`. Служебные строки помечены ⚠️ в колонке «Стабильность». Пометка `(уточнить)` **отсутствует** — все сигнатуры дословно сверены.
7. ✅ Каждая строка `Key methods` дословно сверена по двум источникам — см. таблицу Verification log выше; `src/cf/CommonModules/.../Module.bsl` использован как приоритет, `bsp3111_md/...md` как дополнительный. Расхождений `bsp3111_md/...` ↔ `src/cf/...` **не обнаружено** (полные сигнатуры совпадают).
8. ✅ `## Patterns` содержит **5** примеров с кодом на BSL (серверный замер; замер длительной операции; простая регистрация; счётчик с уникальностью; условное выполнение). Минимум 3 — выполнено.
9. ✅ `## Anti-patterns` содержит **7** пар «❌ / ✅» (замер через `ТекущаяДата()`; выдуманный `ЗафиксироватьДлительностьКлючевойОперации`; выдуманный `ЗаписатьОперацию`; `ЗакончитьЗамерВремени` без `Начать`; `ОценкаПроизводительности.НачатьЗамерВремени` на клиенте; лишний `Если ЦентрМониторингаВключен`; прямой доступ к `БуферОперацийСтатистики`). Минимум 5 — выполнено.
10. ✅ `## How to explore deeper` указывает: 8 имён общих модулей для углубления; 4 grep-шаблона; 4 glob-маски; mermaid-диаграмма зависимостей.
11. ✅ В скиле **нет** ссылок на `bsp3111_md/` (0 совпадений), `its.1c.ru` (0), `1c.ru` (0), `src/cf/` (0). Нет бинарных ассетов. Проверено программно: `grep -c` для всех запрещённых паттернов возвращает 0.
12. ✅ Объём `SKILL.md` = **417 строк / ~47 824 байт** ≤ 500 строк. Progressive disclosure не требуется (скил укладывается в лимит).
13. ✅ Self-review по `prompt-crafting-guide`: frontmatter соответствует `AgentSkills.io`; description — только триггеры, без пересказа workflow; нет эмодзи; ссылки на другие скилы (`bsp-fundamentals`, `bsp-base-common`) в `related_skills` без `@`-force-load; таблицы — компактные; mermaid-диаграмма — текстовая, без изображений.

## Baseline log (RED-фаза, §9.0)

- **Статус:** ❌ не выполнялась.
- **Причина:** согласно `plans/2026-06-06-bsp-skills-roadmap.md` §0.1 п.1, RED-фаза требует запуска `opencode run` с `--runs 1..3` и 3 задачами из `## When to use`. Задача этого прогона — **только** статическая верификация (см. инструкцию пользователя: «Use static verification. Do not run opencode run / smoke tests»).
- **Решение по §9.0:** RED-фаза будет выполнена отдельным прогоном (вне статической верификации) перед финальной отметкой чек-бокса.
- **Допущения:** поскольку 5 из 5 методов в §6.6 имели заглушку `(уточнить)`, baseline-агенту без скила пришлось бы угадывать имена. Скил снимает эту неоднозначность (что и является его ценностью), но RED-прогон нужен для подтверждения снижения числа «своих» реализаций.

## Smoke-test log (GREEN-фаза, §9.2 / §9.4)

- **Статус:** ❌ не выполнялась.
- **Причина:** аналогично RED-фазе — статическая верификация по запросу пользователя.
- **Будет выполнено:** 3 задачи из `## When to use` через `opencode-runner` `references/test-harness.sh` с `--skill-type reference` (auto-detect L2 по числу backtick-имён методов) и `--runs 1` (минимум для L2, не L0).

## REFACTOR log

**Не выполнялся** (RED-фаза не запускалась; рефакторить нечего).

**Предварительные гипотезы** (для будущего REFACTOR после RED-прогона):

- Если агенты в RED выдумывают `ОценкаПроизводительности.ЗафиксироватьДлительностьКлючевойОперации` — усилить Anti-patterns #2 (добавить ссылку на правильный путь в первой строке).
- Если агенты путают `ОценкаПроизводительности` (сервер) и `ОценкаПроизводительностиКлиент` (клиент) — добавить Mermaid-стрелку с указанием контекста в Core concepts.
- Если агенты забывают `КлючУникальности` в `ЗаписатьОперациюБизнесСтатистикиЧас` — расширить Pattern #4 пояснением про уникальность в рамках часа.
- Если агенты не понимают, зачем нужен `ВыполненСОшибкой` — добавить в Pattern #1 вызов с `Истина` в блоке `Исключение`.

**Закрытые лазейки** (на этапе GREEN, до RED-прогона, по итогам анализа §6.6 и src/cf/):

- ❌ → ✅ в Anti-patterns #2: имя `ЗафиксироватьДлительностьКлючевойОперации` помечено как «не существует» с указанием реальной замены.
- ❌ → ✅ в Anti-patterns #3: имя `ЗаписатьОперацию` помечено как «не существует» с указанием полного имени.
- ❌ → ✅ в Anti-patterns #5: явно показано, что `ОценкаПроизводительности.НачатьЗамерВремени` **не работает на клиенте** (проверено по `Module.xml`: `<ClientManagedApplication>false</ClientManagedApplication>`).
- ❌ → ✅ в Anti-patterns #6: `ЗаписатьОперациюБизнесСтатистики` сам гейтит запись — лишний `Если` помечен как шум.

### Итоговая сводка DoD

| Раздел DoD | Статус |
|---|---|
| `## Verification log` | ✅ заполнен (14 строк + таблица расхождений плана) |
| `## Self-check §9.1` | ✅ все 13 пунктов ✅ |
| `## Baseline log` | ⏸ RED-фаза не запускалась (статическая верификация по запросу) |
| `## Smoke-test log` | ⏸ GREEN-фаза не запускалась (статическая верификация по запросу) |
| `## REFACTOR log` | ⏸ не выполнялся (зависит от RED/GREEN) |
