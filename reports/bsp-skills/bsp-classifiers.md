# bsp-classifiers — DoD-отчёт

> Служебный файл. **Не входит в комплект поставки скила**, на `SKILL.md` не ссылается.
> Хранится только в этом репозитории для аудита качества и улучшения скилов.

## Verification log

Сверка Key methods против исходного кода модулей `КалендарныеГрафики`, `ГрафикиРаботы` и `КалендарныеГрафикиСлужебный`. Файлы: `src/cf/CommonModules/КалендарныеГрафики/Ext/Module.bsl`, `src/cf/CommonModules/ГрафикиРаботы/Ext/Module.bsl`, `src/cf/CommonModules/КалендарныеГрафикиСлужебный/Ext/Module.bsl`. Сверка проведена статически через `grep -nE "^(Функция|Процедура) [А-Я]...\(.*\) Экспорт"` + чтение соответствующих строк.

| # | Метод | Сигнатура в `SKILL.md` | Сигнатура в `Module.bsl` | Статус |
|---|---|---|---|---|
| 1 | `КалендарныеГрафики.ДатаПоКалендарю` | `ДатаПоКалендарю(Знач ГрафикРаботы, Знач ДатаОт, Знач КоличествоДней, ВызыватьИсключение = Истина)` | `Функция ДатаПоКалендарю(Знач ГрафикРаботы, Знач ДатаОт, Знач КоличествоДней, ВызыватьИсключение = Истина) Экспорт` (строка 126) | ✅ verified дословно |
| 2 | `КалендарныеГрафики.ДатыПоКалендарю` | `ДатыПоКалендарю(Знач ГрафикРаботы, Знач ДатаОт, Знач МассивДней, Знач РассчитыватьСледующуюДатуОтПредыдущей = Ложь, ВызыватьИсключение = Истина)` | `Функция ДатыПоКалендарю(Знач ГрафикРаботы, Знач ДатаОт, Знач МассивДней, Знач РассчитыватьСледующуюДатуОтПредыдущей = Ложь, ВызыватьИсключение = Истина) Экспорт` (строка 28) | ✅ verified дословно |
| 3 | `КалендарныеГрафики.РазностьДатПоКалендарю` | `РазностьДатПоКалендарю(Знач ГрафикРаботы, Знач ДатаНачала, Знач ДатаОкончания, ВызыватьИсключение = Истина)` | `Функция РазностьДатПоКалендарю(Знач ГрафикРаботы, Знач ДатаНачала, Знач ДатаОкончания, ВызыватьИсключение = Истина) Экспорт` (строка 164) | ✅ verified дословно |
| 4 | `КалендарныеГрафики.БлижайшиеРабочиеДаты` | `БлижайшиеРабочиеДаты(ПроизводственныйКалендарь, НачальныеДаты, ПараметрыПолучения = Неопределено)` | `Функция БлижайшиеРабочиеДаты(ПроизводственныйКалендарь, НачальныеДаты, ПараметрыПолучения = Неопределено) Экспорт` (строка 301) | ✅ verified дословно |
| 5 | `КалендарныеГрафики.ПараметрыПолученияБлижайшихРабочихДат` | `ПараметрыПолученияБлижайшихРабочихДат(ПроизводственныйКалендарь = Неопределено)` | `Функция ПараметрыПолученияБлижайшихРабочихДат(ПроизводственныйКалендарь = Неопределено) Экспорт` (строка 272) | ✅ verified дословно |
| 6 | `КалендарныеГрафики.ОсновнойПроизводственныйКалендарь` | `ОсновнойПроизводственныйКалендарь()` | `Функция ОсновнойПроизводственныйКалендарь() Экспорт` (строка 431) | ✅ verified дословно |
| 7 | `КалендарныеГрафики.ЗаполнитьПроизводственныйКалендарьВФорме` | `ЗаполнитьПроизводственныйКалендарьВФорме(Форма, ПутьРеквизита, КПП = Неопределено)` | `Процедура ЗаполнитьПроизводственныйКалендарьВФорме(Форма, ПутьРеквизита, КПП = Неопределено) Экспорт` (строка 409) | ✅ verified дословно |
| 8 | `ГрафикиРаботы.ДатаПоГрафику` | `ДатаПоГрафику(Знач ГрафикРаботы, Знач ДатаОт, Знач КоличествоДней, ВызыватьИсключение = Истина)` | `Функция ДатаПоГрафику(Знач ГрафикРаботы, Знач ДатаОт, Знач КоличествоДней, ВызыватьИсключение = Истина) Экспорт` (строка 104) | ✅ verified дословно |
| 9 | `ГрафикиРаботы.БлижайшиеДатыВключенныеВГрафик` | `БлижайшиеДатыВключенныеВГрафик(ГрафикРаботы, НачальныеДаты, ПараметрыПолучения = Неопределено)` | `Функция БлижайшиеДатыВключенныеВГрафик(ГрафикРаботы, НачальныеДаты, ПараметрыПолучения = Неопределено) Экспорт` (строка 161) | ✅ verified дословно |
| 10 | `КалендарныеГрафикиСлужебный.ОбновитьДанныеЗависимыеОтПроизводственныхКалендарей` | `ОбновитьДанныеЗависимыеОтПроизводственныхКалендарей(Знач УсловияОбновления, Знач ИдентификаторФайла)` | `Процедура ОбновитьДанныеЗависимыеОтПроизводственныхКалендарей(Знач УсловияОбновления, Знач ИдентификаторФайла) Экспорт` (строка 57) | ✅ verified дословно, ⚠️ служебный API |

**Итого:** 10/10 ✅ verified дословно, все сигнатуры сверены с исходным кодом модулей.

**Дополнительно сверено (проверка родственных методов, не вошедших в Key methods):**

- ⚠️ `КалендарныеГрафики.РасписанияРаботыНаПериод` (строка 369) — `Функция РасписанияРаботыНаПериод(Графики, ДатаНачала, ДатаОкончания) Экспорт`. **Делегирует в `ГрафикиРаботы.РасписанияРаботыНаПериод`** при наличии подсистемы `ГрафикиРаботы`, иначе бросает исключение. Упомянут в Pattern 5 и Anti-patterns с пометкой ⚠️ «требует подсистему ГрафикиРаботы».
- ⚠️ `КалендарныеГрафики.ПериодыНерабочихДней` (строка 456) — `Функция ПериодыНерабочихДней(ПроизводственныйКалендарь, ПериодОтбор) Экспорт`. Делегирует в `Обработки.ЗаполнениеКалендарныхГрафиков.ПериодыНерабочихДней`. Упомянут в Core concepts (раздел про `РегистрСведений.ПериодыНерабочихДнейКалендаря`), в Key methods не включён из-за узкой применимости (нужен при работе с `БлижайшиеРабочиеДаты` с фильтром по конкретным нерабочим периодам).
- ⚠️ `КалендарныеГрафики.ДатыБлижайшихРабочихДней` (строка 546) — **устаревший** метод (`#Область УстаревшиеПроцедурыИФункции`), делегирует в `БлижайшиеРабочиеДаты` / `БлижайшиеДатыВключенныеВГрафик`. Закрыт в Anti-patterns (последний пункт).
- ⚠️ `КалендарныеГрафики.СоздатьВТРасписанияРаботыНаПериод` (строка 390) — `Процедура СоздатьВТРасписанияРаботыНаПериод(МенеджерВременныхТаблиц, Графики, ДатаНачала, ДатаОкончания) Экспорт`. Упомянут в Pattern 5 как альтернатива `РасписанияРаботыНаПериод` для больших объёмов.
- ⚠️ `ГрафикиРаботы.РазностьДатПоКалендарю` (строка 529) — `Функция РазностьДатПоКалендарю(Знач ГрафикРаботы, Знач ДатаНачала, Знач ДатаОкончания, ВызыватьИсключение = Истина) Экспорт`. **Одноимённый** с `КалендарныеГрафики.РазностьДатПоКалендарю`, но принимает **только** `СправочникСсылка.Календари`. В `КалендарныеГрафики.РазностьДатПоКалендарю` есть делегация внутрь. Закрыт в Anti-patterns (п.3 — не путать типы ссылок).
- ⚠️ `ГрафикиРаботы.ПараметрыПолученияБлижайшихДатПоГрафику` (строка 138) — конструктор параметров для `БлижайшиеДатыВключенныеВГрафик`. Упомянут в Core concepts косвенно (через упоминание структуры `ПараметрыПолучения`); в Key methods не включён из-за узкой применимости (аналог `КалендарныеГрафики.ПараметрыПолученияБлижайшихРабочихДат` для графиков).

**Дополнительно подтверждено:**

- ✅ **Модуль `КалендарныеГрафики` существует и содержит 34 экспортных метода** (включая служебные и обработчики событий). Из них 9 — стабильный публичный API в `#Область ПрограммныйИнтерфейс` до `#Область УстаревшиеПроцедурыИФункции` (8 расчётных + 1 `СоздатьВТРасписанияРаботыНаПериод`); 1 — устаревший (`ДатыБлижайшихРабочихДней`); остальные — обработчики событий / классификаторов / обновления ИБ. **Модуля `КалендарныеГрафикиКлиент` НЕ существует** — упомянуто в Core concepts.
- ✅ **Модуль `ГрафикиРаботы` существует и содержит 15 экспортных методов** (расчётные + обработчики событий). Модуль `ГрафикиРаботыКлиент` существует, но содержит только 3 экспортные утилиты для UI-коллекций (`ВосстановитьПорядокСтрокКоллекцииПослеРедактирования`, `ВставитьВФиксированноеСоответствие`, `УдалитьИзФиксированногоСоответствия`), **не** для расчёта дат — упомянуто в Core concepts.
- ✅ **Модуль `КалендарныеГрафикиПереопределяемый` содержит 4 крючка**: `ПриОбновленииПроизводственныхКалендарей` (строка 19), `ПриОбновленииДанныхЗависимыхОтПроизводственныхКалендарей` (строка 31), `ПриЗаполненииБлокируемыхОбъектовЗависимыеОтПроизводственныхКалендарей` (строка 42), `ПриЗаполненииИзменяемыхОбъектовЗависимыеОтПроизводственныхКалендарей` (строка 53). Упомянуты в Core concepts и Anti-patterns (п.4) как место для прикладной логики реакции на обновление.
- ✅ **Модуль `КалендарныеГрафикиСлужебный` содержит 3 экспортных метода** — все служебные. Упомянуто в Core concepts с явной пометкой ⚠️ «служебный API, обратная совместимость не гарантируется».
- ✅ Состав подсистемы `КалендарныеГрафики` (из `.xml`): общий модуль `КалендарныеГрафики`, регистр `ДанныеПроизводственногоКалендаря`, справочник `ПроизводственныеКалендари`, перечисление `ВидыДнейПроизводственногоКалендаря`, константа и ФО `ИспользоватьНесколькоПроизводственныхКалендарей`, обработка `ЗаполнениеКалендарныхГрафиков`, регистр `ПериодыНерабочихДнейКалендаря`. Упомянуто в Core concepts.
- ✅ Состав подсистемы `ГрафикиРаботы` (из `.xml`): общие модули `ГрафикиРаботы` + `ГрафикиРаботыКлиент`, регистры `КалендарныеГрафики` + `РучныеИзмененияГрафиковРаботы`, справочник `Календари`, обработка `ЗаполнениеГрафиковРаботы`, определяемый тип `ВладелецГрафикаРаботы`. Упомянуто в Core concepts.
- ✅ Подсистемы верхнего уровня с именами `ПроизводственныйКалендарь` **НЕ существует** — производственный календарь это справочник (`Catalog.ПроизводственныеКалендари`), входящий в подсистему `КалендарныеГрафики`. В `related_subsystems` `ПроизводственныйКалендарь` указан как **сущность**, а не подсистема (согласно заданию — «(related)»).

## Self-check §9.1

| # | Пункт | Статус | Пояснение |
|---|---|---|---|
| 1 | Файл `.claude/skills/bsp-classifiers/SKILL.md` существует | ✅ | Создан, 329 строк (≤ 500 лимит) |
| 2 | Frontmatter валиден (name, description, metadata) | ✅ | `name: bsp-classifiers`, `description` только триггеры, начинается с `Use this skill when…`, без кириллицы, third person. `metadata` заполнен: `scope/version/layer/related_subsystems/related_skills` (включая `bsp-fundamentals` и `bsp-base-common`) |
| 3 | `## When to use` содержит ≥ 3 типичных сценария | ✅ | 6 сценариев (глагольная форма: «рассчитать дату на N рабочих дней», «посчитать количество рабочих дней», «сдвинуть дату к ближайшему рабочему дню», «определить основной производственный календарь / заполнить реквизит в форме», «составить расписание работы на период», «обновить данные зависимых от календаря объектов») |
| 4 | `## Не использовать, если` содержит ≥ 2 анти-триггера | ✅ | 5 анти-триггеров (загрузка классификаторов из внешнего сервиса, разность произвольных дат в календарных днях, табелирование учёта рабочего времени, запись/чтение ссылки на элемент справочника без расчёта, справочник банков/валют — другой L3-скил) |
| 5 | `## Core concepts` объясняет архитектуру | ✅ | Три сущности (календарь / график / расписание), где живут модули (5 имён модулей), регистры сведений (4), доступность (Сервер/Толстый/Внешнее), виды дней, функциональная опция «Несколько производственных календарей» |
| 6 | Таблица `## Key methods` 5–10 строк (для L3), все колонки, `(уточнить)` запрещён | ✅ | 10 строк. Колонки: Метод / Сигнатура / Сервер-Клиент / Назначение / Пример вызова. Все сигнатуры дословно сверены, `(уточнить)` отсутствует. ⚠️-пометка для `КалендарныеГрафикиСлужебный` проставлена |
| 7 | Каждая строка Key methods сверена по двум источникам | ✅ | см. Verification log выше — 10/10 сверены дословно с `Module.bsl`, для каждого указана строка |
| 8 | `## Patterns` ≥ 3 примеров с BSL-кодом | ✅ | 5 паттернов: расчёт плановой даты с fallback, цепочка связанных дат (`РассчитыватьСледующуюДатуОтПредыдущей = Истина`), подсчёт рабочих дней в периоде, ближайший рабочий день с учётом нерабочих периодов, расписание работы на день |
| 9 | `## Anti-patterns` ≥ 5 пар «❌ / ✅» | ✅ | 6 пар: ручной цикл по календарю, константа «единственного» календаря, смешивание `ПроизводственныеКалендари` и `Календари`, прямой вызов служебного модуля, заполнение формы без КПП, использование устаревшего `ДатыБлижайшихРабочихДней` |
| 10 | `## How to explore deeper` для L3: имена модулей + ≥ 1 grep-pattern | ✅ | 11 объектов метаданных (5 модулей + 2 subsystem.xml + 4 регистра + 2 справочника + 1 перечисление + 1 обработка) + 3 grep-шаблона + mermaid-диаграмма (L3 минимум — имена модулей + 1 grep, превышение не запрещено) |
| 11 | Нет ссылок на bsp3111_md, its.1c.ru, 1c.ru, src/cf, нет бинарных ассетов | ✅ | Проверено: `grep -nE "bsp3111_md|its\.1c\.ru|1c\.ru|src/cf" .claude/skills/bsp-classifiers/SKILL.md` — 0 совпадений. В SKILL.md — только имена модулей (через `CommonModules/...` / `Subsystems/...` без привязки к конкретной корневой папке), имена скилов, номера строк **не упоминаются** |
| 12 | Объём SKILL.md ≤ 500 строк / 5000 токенов | ✅ | 329 строк (целевой предел 500, aim 200–300, факт — 329 — в допустимом диапазоне) |
| 13 | Self-review по чек-листу `prompt-crafting-guide` | ✅ | Description оптимизирован под CSO (только триггеры, без workflow-summary, без кириллицы), no menus, no key-binding lists, only specific BSL code examples, ошибки с явной диагностикой (что не так и почему), defaults given (например, `ВызыватьИсключение = Истина`, `РассчитыватьСледующуюДатуОтПредыдущей = Ложь`), каждый паттерн начинается с контекста «когда нужно» |

## Baseline log (RED-фаза)

**Не проводился в этой итерации.** RED/GREEN-прогоны через `opencode-runner/test-harness.sh` — задача следующей итерации после развёртывания скила. Текущая итерация — статическая верификация (Verification log) + ручной self-check.

См. инструкции §9.0 (3 задачи из `## When to use` + `--runs 3` majority vote для устойчивых метрик на flaky-моделях, в той же `--dir` для RED и GREEN).

## Smoke-test log (GREEN-фаза)

**Не проводился в этой итерации.** Запуск `test-harness.sh` — задача следующей итерации.

## REFACTOR log

**Anti-patterns, закрытые при написании скила** (на основании сверки с roadmap §7 и реальным кодом модулей):

1. ✅ Закрыт фантом `КалендарныеГрафикиКлиент` (не существует) — явно упомянут в Core concepts с пояснением, что в 3.1.11 клиентского варианта нет, расчётные методы доступны только с Сервера/Толстого/Внешнего соединения.
2. ✅ Закрыта путаница `КалендарныеГрафики.РазностьДатПоКалендарю` vs `ГрафикиРаботы.РазностьДатПоКалендарю` — оба реальны, оба упомянуты в Verification log как «одноимённые», различие по типу первого аргумента (`ПроизводственныеКалендари` vs `Календари`) вынесено в Anti-patterns п.3.
3. ✅ Уточнено, что `КалендарныеГрафики.БлижайшиеРабочиеДаты` принимает **только** `СправочникСсылка.ПроизводственныеКалендари` (не `Календари`) — упомянуто в Core concepts и закрыто в Anti-patterns п.3. Для графиков — `ГрафикиРаботы.БлижайшиеДатыВключенныеВГрафик`.
4. ✅ Закрыт устаревший `КалендарныеГрафики.ДатыБлижайшихРабочихДней` — не включён в Key methods, явно упомянут в Verification log и Anti-patterns п.6 с указанием стабильных замен.
5. ✅ Закрыт фантом «константа ОсновнойПроизводственныйКалендарь» — анти-паттерн п.2 показывает, что корректный путь — `КалендарныеГрафики.ОсновнойПроизводственныйКалендарь()` или `ЗаполнитьПроизводственныйКалендарьВФорме` с КПП.
6. ✅ Закрыт анти-паттерн «ручной цикл по календарю через `ДеньНедели()`» — анти-паттерн п.1 с пояснением, что праздники и переносы выходных не учитываются.
7. ✅ Помечен служебный модуль `КалендарныеГрафикиСлужебный` ⚠️ — единственный метод в Key methods из этого модуля явно помечен ⚠️, в Core concepts модуль описан с предупреждением.
8. ✅ Указано, что `ЗаполнитьПроизводственныйКалендарьВФорме` инкапсулирует логику функциональной опции «Несколько производственных календарей» — анти-паттерн п.5 показывает, что при множественных календарях обязательно передавать КПП.
9. ✅ Добавлен Pattern 5 с предупреждением, что `РасписанияРаботыНаПериод` требует подсистему `ГрафикиРаботы` — иначе бросает исключение.
10. ✅ В `related_subsystems` указана сущность `ПроизводственныйКалендарь` (не подсистема), что соответствует фактической структуре метаданных (производственный календарь — это справочник в подсистеме `КалендарныеГрафики`).
