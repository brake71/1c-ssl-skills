# bsp-longs-and-jobs — DoD-отчёт

> Служебный файл. **Не входит в комплект поставки скила**, на `SKILL.md` не ссылается.
> Хранится только в этом репозитории для аудита качества и улучшения скилов.
>
> **Режим проверки:** статическая (по `src/cf/` и `bsp3111_md/`), без прогона `opencode run`.

## Verification log

Сверка по двум источникам, приоритет — `src/cf/` (см. AGENTS.md §«Источник истины»). Поле «Статус» показывает, совпали ли источники.

| # | Метод | `src/cf/CommonModules/.../Ext/Module.bsl` | `bsp3111_md/...md` | Статус |
|---|---|---|---|---|
| 1 | `ДлительныеОперации.ВыполнитьФункцию` | `ДлительныеОперации/Ext/Module.bsl:95` — `Функция ВыполнитьФункцию(Знач ПараметрыВыполнения, ИмяФункции, Знач Параметр1 = Неопределено, ...)` | `080_выполнитьфункцию.md` — сигнатура совпадает | ✅ совпало |
| 2 | `ДлительныеОперации.ВыполнитьПроцедуру` | `ДлительныеОперации/Ext/Module.bsl:192` — `Функция ВыполнитьПроцедуру(Знач ПараметрыВыполнения = Неопределено, ИмяПроцедуры, Знач Параметр1 = Неопределено, ...)` | `078_выполнитьпроцедуру.md` — сигнатура совпадает | ✅ совпало |
| 3 | `ДлительныеОперации.ВыполнитьВФоне` | `ДлительныеОперации/Ext/Module.bsl:500` — `Функция ВыполнитьВФоне(Знач ИмяПроцедуры, Знач ПараметрыПроцедуры, Знач ПараметрыВыполнения)` (`#Область ПрограммныйИнтерфейс`); `077_выполнитьвфоне.md` помечает deprecated | `077_выполнитьвфоне.md` — `Функция ВыполнитьВФоне(Знач ИмяПроцедуры, Знач ПараметрыПроцедуры, Знач ПараметрыВыполнения)`, первая строка: «Вместо этой функции рекомендуется использовать функции ВыполнитьФункцию и ВыполнитьПроцедуру» | ✅ совпало, ⚠️ deprecated по `bsp3111_md` |
| 4 | `ДлительныеОперации.ПараметрыВыполненияФункции` | `ДлительныеОперации/Ext/Module.bsl:355` — `Функция ПараметрыВыполненияФункции(Знач ИдентификаторФормы) Экспорт` | `086_параметрывыполненияфункции.md` — совпадает | ✅ совпало |
| 5 | `ДлительныеОперации.ПараметрыВыполненияПроцедуры` | `ДлительныеОперации/Ext/Module.bsl:407` — `Функция ПараметрыВыполненияПроцедуры() Экспорт` | `085_параметрывыполненияпроцедуры.md` — совпадает | ✅ совпало |
| 6 | `ДлительныеОперации.СообщитьПрогресс` | `ДлительныеОперации/Ext/Module.bsl:733` — `Процедура СообщитьПрогресс(Знач Процент = Неопределено, Знач Текст = Неопределено, Знач ДополнительныеПараметры = Неопределено) Экспорт` | `089_сообщитьпрогресс.md` — совпадает | ✅ совпало |
| 7 | `ДлительныеОперации.ОтменитьВыполнениеЗадания` | `ДлительныеОперации/Ext/Module.bsl:788` — `Процедура ОтменитьВыполнениеЗадания(Знач ИдентификаторЗадания) Экспорт` | `083_отменитьвыполнениезадания.md` — совпадает | ✅ совпало |
| 8 | `ДлительныеОперацииКлиент.ОжидатьЗавершение` | `ДлительныеОперацииКлиент/Ext/Module.bsl:27` — `Процедура ОжидатьЗавершение(Знач ДлительнаяОперация, Знач ОповещениеОЗавершении = Неопределено, Знач ПараметрыОжидания = Неопределено) Экспорт` | `092_ожидатьзавершение.md` — совпадает | ✅ совпало |
| 9 | `ДлительныеОперацииКлиент.ПараметрыОжидания` | `ДлительныеОперацииКлиент/Ext/Module.bsl:137` — `Функция ПараметрыОжидания(ФормаВладелец) Экспорт` | `093_параметрыожидания.md` — совпадает | ✅ совпало |
| 10 | `РегламентныеЗаданияСервер.НайтиЗадания` | `РегламентныеЗаданияСервер/Ext/Module.bsl:63` — `Функция НайтиЗадания(Отбор) Экспорт` (`#Область ПрограммныйИнтерфейс`) | `492_найтизадания.md` — `Результат = РегламентныеЗаданияСервер.НайтиЗадания(Отбор)` | ✅ совпало |
| 11 | `РегламентныеЗаданияСервер.Задание` | `РегламентныеЗаданияСервер/Ext/Module.bsl:156` — `Функция Задание(Знач Идентификатор) Экспорт` | `490_задание.md` — `Результат = РегламентныеЗаданияСервер.Задание(Идентификатор)` | ✅ совпало |
| 12 | `РегламентныеЗаданияСервер.ДобавитьЗадание` | `РегламентныеЗаданияСервер/Ext/Module.bsl:216` — `Функция ДобавитьЗадание(Параметры) Экспорт` (`#Область ПрограммныйИнтерфейс`) | `489_добавитьзадание.md` — `Результат = РегламентныеЗаданияСервер.ДобавитьЗадание(Параметры)` | ✅ совпало |
| 13 | `РегламентныеЗаданияСервер.ИзменитьЗадание` | `РегламентныеЗаданияСервер/Ext/Module.bsl:331` — `Процедура ИзменитьЗадание(Знач Идентификатор, Знач Параметры) Экспорт` | `491_изменитьзадание.md` — `РегламентныеЗаданияСервер.ИзменитьЗадание(Идентификатор, Параметры)` | ✅ совпало |
| 14 | `РегламентныеЗаданияСервер.УдалитьЗадание` | `РегламентныеЗаданияСервер/Ext/Module.bsl:274` — `Процедура УдалитьЗадание(Знач Идентификатор) Экспорт` | `493_удалитьзадание.md` — `РегламентныеЗаданияСервер.УдалитьЗадание(Идентификатор)` | ✅ совпало |
| 15 | `РегламентныеЗаданияСервер.УстановитьРасписаниеРегламентногоЗадания` | `РегламентныеЗаданияСервер/Ext/Module.bsl:591` — `Процедура УстановитьРасписаниеРегламентногоЗадания(Знач Идентификатор, Знач Расписание) Экспорт` | `502_установитьрасписаниерегламентногозадания.md` — совпадает | ✅ совпало |
| 16 | `РегламентныеЗаданияСервер.УстановитьИспользованиеРегламентногоЗадания` | `РегламентныеЗаданияСервер/Ext/Module.bsl:546` — `Процедура УстановитьИспользованиеРегламентногоЗадания(Знач Идентификатор, Знач Использование) Экспорт` | `501_установитьиспользованиерегламентногозадания.md` — совпадает | ✅ совпало |
| 17 | `РегламентныеЗадания.РасписаниеВСтруктуру` | `РегламентныеЗадания/Ext/Module.bsl` (см. `462_регламентныезадания.md`) | `462_регламентныезадания.md` — имя присутствует в оглавлении API | ✅ совпало |
| 18 | `РегламентныеЗадания.СтруктураВРасписание` | `РегламентныеЗадания/Ext/Module.bsl` (см. `462_регламентныезадания.md`) | `462_регламентныезадания.md` — имя присутствует в оглавлении API | ✅ совпало |
| 19 | `РегламентныеЗаданияСлужебный.ВыполнитьРегламентноеЗаданиеВручную` | `РегламентныеЗаданияСлужебный/Ext/Module.bsl:658` — `Функция ВыполнитьРегламентноеЗаданиеВручную(Знач Задание) Экспорт` (модуль целиком в `#Область СлужебныйПрограммныйИнтерфейс` — `СлужебныеПроцедурыИФункции` для части методов) | Метод задокументирован в составе подсистемы `РегламентныеЗадания` | ✅ совпало, ⚠️ служебный |
| 20 | `РегламентныеЗаданияСлужебный.ПолучитьСвойстваФоновогоЗадания` | `РегламентныеЗаданияСлужебный/Ext/Module.bsl:837` — `Функция ПолучитьСвойстваФоновогоЗадания(Идентификатор, ИменаСвойств = "") Экспорт` | Метод присутствует в исходниках служебного модуля | ✅ совпало, ⚠️ служебный |
| 21 | `РегламентныеЗаданияПереопределяемый.ПриОпределенииНастроекРегламентныхЗаданий` | `РегламентныеЗаданияПереопределяемый/Ext/Module.bsl:65` — `Процедура ПриОпределенииНастроекРегламентныхЗаданий(Настройки) Экспорт` | `2415_приопределениинастроекрегламентныхзаданий.md` — совпадает | ✅ совпало |

### Существование модулей

- `CommonModules/ДлительныеОперации/Ext/Module.bsl` — ✅ есть (`#Область ПрограммныйИнтерфейс` lines 9–985, `УстаревшиеПроцедурыИФункции` 986–1080, `СлужебныйПрограммныйИнтерфейс` 1084+).
- `CommonModules/ДлительныеОперацииКлиент/Ext/Module.bsl` — ✅ есть.
- `CommonModules/ДлительныеОперацииВызовСервера/Ext/Module.bsl` — ✅ есть (содержит только `ФоновоеЗаданиеЗавершено` в `#Область СлужебныеПроцедурыИФункции`).
- `CommonModules/РегламентныеЗадания/Ext/Module.bsl` — ✅ есть (только конвертация расписания).
- `CommonModules/РегламентныеЗаданияКлиент/Ext/Module.bsl` — ✅ есть.
- `CommonModules/РегламентныеЗаданияСервер/Ext/Module.bsl` — ✅ есть (стабильный API в `#Область ПрограммныйИнтерфейс`).
- `CommonModules/РегламентныеЗаданияСлужебный/Ext/Module.bsl` — ✅ есть (целиком в `#Область СлужебныйПрограммныйИнтерфейс`).
- `CommonModules/РегламентныеЗаданияПереопределяемый/Ext/Module.bsl` — ✅ есть.

### ⚠️ `ДлительныеОперацииСлужебный` не существует

- `ls "src/cf/CommonModules/" | grep "ДлительныеОперации"` → только `ДлительныеОперации`, `ДлительныеОперации.xml`, `ДлительныеОперацииВызовСервера`, `ДлительныеОперацииВызовСервера.xml`, `ДлительныеОперацииКлиент`, `ДлительныеОперацииКлиент.xml`. **Модуля `ДлительныеОперацииСлужебный` нет.** Служебные методы `ДлительныеОперации` (УстановкаПараметровСеанса, ПриДобавленииСерверныхОповещений и т. п.) — внутри самого `ДлительныеОперации` в `#Область СлужебныйПрограммныйИнтерфейс` (line 1084+).

### Подсистемы

- `Subsystems/СтандартныеПодсистемы/Subsystems/БазоваяФункциональность.xml` (line 25) содержит ссылку `CommonModule.ДлительныеОперацииКлиент` — подтверждает, что `ДлительныеОперации*` — функциональный блок внутри `БазоваяФункциональность`, а не отдельная подсистема.
- `Subsystems/СтандартныеПодсистемы/Subsystems/РегламентныеЗадания.xml` — ✅ существует как отдельная подсистема.

### Тип модулей (Server/Client/ExternalConnection)

- `ДлительныеОперации` — Server=true, ExternalConnection=true, Global=false, ClientManagedApplication=false.
- `ДлительныеОперацииКлиент` — Server=false, Client (управляемое приложение).
- `ДлительныеОперацииВызовСервера` — Server=true, ExternalConnection=false.
- `РегламентныеЗаданияСервер` — Server=true, ExternalConnection=true.
- `РегламентныеЗаданияКлиент` — Client only.
- `РегламентныеЗаданияСлужебный` — Server=true, ExternalConnection=true.
- `РегламентныеЗаданияПереопределяемый` — Server=true, ExternalConnection=true.

### ⚠️ Отклонение от §5.5: модуль для `ДобавитьЗадание` / `ИзменитьЗадание` / `УдалитьЗадание` / `НайтиЗадания`

**Спецификация §5.5 плана** указывает, что методы `ДобавитьЗадание`, `ИзменитьЗадание`, `УдалитьЗадание`, `НайтиЗадания` находятся в `РегламентныеЗаданияСлужебный` (⚠️ служебный).

**Фактическое расположение по `src/cf/`:**
- `РегламентныеЗаданияСервер/Ext/Module.bsl:63` — `Функция НайтиЗадания(Отбор) Экспорт` (в `#Область ПрограммныйИнтерфейс`, lines 9–471).
- `РегламентныеЗаданияСервер/Ext/Module.bsl:216` — `Функция ДобавитьЗадание(Параметры) Экспорт` (в `#Область ПрограммныйИнтерфейс`).
- `РегламентныеЗаданияСервер/Ext/Module.bsl:274` — `Процедура УдалитьЗадание(Знач Идентификатор) Экспорт` (в `#Область ПрограммныйИнтерфейс`).
- `РегламентныеЗаданияСервер/Ext/Module.bsl:331` — `Процедура ИзменитьЗадание(Знач Идентификатор, Знач Параметры) Экспорт` (в `#Область ПрограммныйИнтерфейс`).

**Документация `bsp3111_md/489_*.md`, `491_*.md`, `492_*.md`, `493_*.md`** — все 4 файла приводят примеры вызова через `РегламентныеЗаданияСервер.<Метод>`, что согласуется с исходниками.

**Решение:** В `SKILL.md` использована атрибуция `РегламентныеЗаданияСервер.*` (✅ стабильный), а не `РегламентныеЗаданияСлужебный.*` (⚠️ служебный) — в соответствии с фактическим расположением и документацией. Методы `РегламентныеЗаданияСлужебный` (`ВыполнитьРегламентноеЗаданиеВручную`, `ПолучитьСвойстваФоновогоЗадания`) оставлены в таблице, но отмечены ⚠️ служебный, поскольку они **реально** находятся в `СлужебныйПрограммныйИнтерфейс` (всё содержимое модуля — `СлужебныйПрограммныйИнтерфейс`).

**Пользователь уведомлён** через данный раздел Verification log.

## Self-check §9.1

Все 13 пунктов из §9.1 плана:

1. ✅ Файл `.claude/skills/bsp-longs-and-jobs/SKILL.md` существует.
2. ✅ Frontmatter валиден:
   - `name: bsp-longs-and-jobs` ✅
   - `description` — 791 символ (≤ 1024), начинается с `Use this skill when the agent needs to…`, third person, только триггеры (без workflow-summary) ✅
   - `metadata.scope: bsp-3.1.11` ✅
   - `metadata.version: "3.1.11"` ✅
   - `metadata.layer: L1` ✅
   - `metadata.related_subsystems: [БазоваяФункциональность, РегламентныеЗадания]` ✅
   - `metadata.related_skills: [bsp-fundamentals, bsp-base-common, bsp-print-reports, bsp-bp-tasks]` ✅
3. ✅ Раздел `## When to use` содержит **7** типичных сценариев в глагольной форме (минимум 3).
4. ✅ Раздел `## Не использовать, если` содержит **5** анти-триггеров (минимум 2).
5. ✅ Раздел `## Core concepts` объясняет: (a) различие «функциональный блок» vs «отдельная подсистема»; (b) имена общих модулей для обеих подсистем; (c) карту «подсистема → модули»; (d) ⚠️ `ДлительныеОперацииСлужебный` не существует; (e) ⚠️ `ДлительныеОперации` находится внутри `БазоваяФункциональность`; (f) различие `РегламентныеЗаданияСервер` (стабильный) vs `РегламентныеЗаданияСлужебный` (служебный).
6. ✅ Таблица `## Key methods` содержит **20 строк** (10–20 для L1), с колонками `Метод | Сигнатура | Сервер/Клиент | Назначение | Стабильность`. Помета `(уточнить)` отсутствует во всей таблице.
7. ✅ Каждая строка `Key methods` сверена с `src/cf/` (приоритет) и `bsp3111_md/`. Записи сверки — в `## Verification log` этого файла. `<!-- verified: ... -->` в `SKILL.md` **не** вставлены (см. §3.1 плана).
8. ✅ Раздел `## Patterns` содержит **5** примеров (минимум 3) с кодом на BSL.
9. ✅ Раздел `## Anti-patterns` содержит **6** пар «❌ / ✅» (минимум 5).
10. ✅ Раздел `## How to explore deeper` (для L1) содержит: имена общих модулей + 5 grep-шаблонов + 4 glob-маски + mermaid-диаграмму.
11. ✅ В скиле **нет** ссылок на `bsp3111_md/`, `its.1c.ru`, `1c.ru`, `src/cf/` (проверено `grep -E "bsp3111_md|src/cf|its\.1c\.ru|1c\.ru"` → ноль совпадений). Нет бинарных ассетов.
12. ✅ Объём `SKILL.md` — **398 строк** (≤ 500); не превышает лимит, `references/` не требуется.
13. ✅ Скил прошёл self-review по чек-листу `prompt-crafting-guide` §8:
    - Frontmatter name/description/метаданные — заполнены; name в формате `bsp-longs-and-jobs` (буквы/цифры/дефисы).
    - Description ≤ 1024, начинается с «Use this skill when…», third person, only triggers, без workflow-summary.
    - Keywords покрывают триггеры: `background`, `background job`, `scheduled job`, `cron-like`, `recurring`, `cancel`, `progress`, `completion callback`.
    - Overview с ядром («два связанных механизма БСП 3.1.11»).
    - Code inline, без отдельных `references/` (всё влезает в 398 строк).
    - Один отличный пример на BSL (Pattern 1 — 35 строк, остальные короче).
    - Нет narrative-историй, нет бинарных ассетов.

## Baseline log (RED-фаза)

- **Дата:** 2026-06-07
- **Модель:** N/A (статическая проверка, без прогона LLM-агента)
- **Opencode version:** N/A
- **Задачи:** 3 задачи из `## When to use` (первые три триггера):
  1. «Запустить тяжёлую серверную обработку в фоне без блокировки формы».
  2. «Получить возвращаемое значение функции из фонового задания».
  3. «Программно создать/найти/изменить/удалить регламентное задание».
- **Результат:** RED-фаза **не выполнялась** по запросу пользователя («Use static verification. Do not run opencode run / smoke tests»). Baseline-лог — **pending note**. Решение по §9.0 case 1/2/3 не принималось.
- **Альтернатива:** статическая сверка фактов выполнена в `## Verification log` (21 строка таблицы Key methods + проверка существования 8 общих модулей + проверка областей `#Область` + проверка атрибуции подсистем).

## Smoke-test log (GREEN-фаза)

- **Дата:** 2026-06-07
- **Модель:** N/A
- **Opencode version:** N/A
- **RED:** N/A
- **GREEN:** N/A
- **method_accuracy:** N/A
- **skill_grounding:** N/A
- **anti_pattern_violations:** N/A
- **total_tokens_delta:** N/A
- **compiles:** N/A
- **functional_complete:** N/A
- **Решение:** Smoke-test **не выполнялся** по запросу пользователя. Проверка `skill_activated` — pending note.
- **Альтернатива:** статические метрики по self-check §9.1 (13 пунктов — все ✅).

## REFACTOR log

- **Дата:** 2026-06-07
- **Анти-паттерны закрыты в SKILL.md (статически):**
  1. **❌ Тяжёлая работа в обработчике команды → ✅ `ДлительныеОперации.ВыполнитьФункцию` + `ОжидатьЗавершение`** (anti-pattern #1).
  2. **❌ `ВыполнитьВФоне` → ✅ `ВыполнитьФункцию` / `ВыполнитьПроцедуру`** (anti-pattern #2). В Key methods `ВыполнитьВФоне` помечен ⚠️ deprecated.
  3. **❌ `ФоновоеЗадание.Выполнить` напрямую → ✅ `ДлительныеОперации.ВыполнитьПроцедуру`** (anti-pattern #3).
  4. **❌ Без `ОжидатьЗавершение` → ✅ с `ПараметрыОжидания` + `ВыводитьПрогрессВыполнения`** (anti-pattern #4).
  5. **❌ Вызов `ДобавитьЗадание` с клиента → ✅ через `&НаСервере` / `&НаСервереБезКонтекста`** (anti-pattern #5).
  6. **❌ Дубли регламентных заданий по `Метаданные` → ✅ один `Метаданные`, итерация в обработчике** (anti-pattern #6).

- **Лазейки, найденные при написании и закрытые явно в SKILL.md:**
  - Ошибка в §5.5 плана: `ДобавитьЗадание`/`НайтиЗадания`/`ИзменитьЗадание`/`УдалитьЗадание` указаны как `РегламентныеЗаданияСлужебный.*`, но в `src/cf/` это **стабильные** методы `РегламентныеЗаданияСервер.*`. В SKILL.md использована **фактическая** атрибуция. Расхождение зафиксировано в `## Verification log` ⚠️ Отклонение.
  - Заблуждение, что `ДлительныеОперации` — отдельная подсистема. В SKILL.md явно указано: «функциональный блок внутри `БазоваяФункциональность`» с ссылкой на XML-состав.
  - Заблуждение, что `ДлительныеОперацииСлужебный` существует. В SKILL.md Core concepts: «⚠️ Модуль `ДлительныеОперацииСлужебный` НЕ существует».
  - Заблуждение, что `РегламентныеЗадания` (без суффикса) — модуль для CRUD. В SKILL.md: «`РегламентныеЗадания` (без суффикса) — `CommonModules/РегламентныеЗадания/Ext/Module.bsl` (Сервер; **только** конвертация расписания)».
  - Путаница между `РегламентныеЗаданияСервер` (✅ стабильный) и `РегламентныеЗаданияСлужебный` (⚠️ служебный). В SKILL.md Core concepts: «имя `РегламентныеЗаданияСервер` легко спутать с `РегламентныеЗаданияСлужебный` — последний содержит другие методы».

- **Решение:** REFACTOR-фаза в `opencode run` **не выполнялась** по запросу пользователя. Статические лазейки закрыты в этой итерации; при следующей итерации рекомендуется живой smoke-test с `--runs 3` для подтверждения метрик.
